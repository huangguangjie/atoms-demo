# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，选择主题、构建模式和执行计划，由智能体生成代码并在 App Viewer 中实时预览、交互和发布。平台同时提供资源发现、模板复用、项目管理、社区应用浏览、认证及远程 MCP 连接能力。

当前仓库已从产品原型规划阶段进入可运行前端交付阶段。前端基于 React + Vite 实现，支持演示模式数据回退；Supabase 真实数据接入、数据库建表和 RLS 策略已有设计文档，待完成平台项目关联后启用。应用已完成构建、静态检查和页面级走查，代码及文档已推送至 GitHub。

# Project Module Description

## 应用布局与导航

- 左侧可折叠侧边栏，包含空间选择、首页、资源、我的项目和最近对话。
- 提供社群、免费积分、用户信息、设置和通知入口。
- 主内容区域根据路由切换页面。

## 首页智能体对话

- 支持提示词、附件、主题切换、`#` 引用、构建/目标模式、语音输入及发送/停止。
- 支持远程 MCP 服务配置、连接状态展示和持久化。
- 智能体流程支持计划确认、流式生成、会话消息展示及应用实时预览。
- 当前默认使用演示数据和模拟智能体响应，真实后端能力通过 Supabase 配置接入。

## App Viewer 与发布

- 对话区与应用预览区并列展示。
- 可预览生成应用并进行交互验证。
- Publish 操作位于用户侧 App Viewer 中；平台当前没有可供智能体调用的发布 MCP 工具，发布链接需由用户在 App Viewer 中生成。

## 资源中心

- “发现”展示社区应用，支持分类过滤和体验。
- “模板”展示可复用的应用方案。
- 支持发现与模板 Tab 切换。

## 我的项目

- 展示全部项目和已收藏项目。
- 支持收藏、取消收藏和 Tab 切换。
- 项目数据当前可使用演示模式回退，后续接入 Supabase 实体数据。

## 认证与博客

- 提供认证上下文、认证回调和认证错误页面。
- 支持博客索引、文章详情、Markdown 内容渲染、预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                         # 项目总览与开发入口
├── app/
│   ├── backend/
│   │   ├── README.md                 # 后端配置与开发说明
│   │   └── skills_docs/              # Supabase 开发规范与 Edge Function 说明
│   └── frontend/
│       ├── package.json              # 前端依赖与脚本
│       ├── vite.config.ts            # Vite 配置
│       ├── tailwind.config.ts        # Tailwind 配置
│       ├── components.json           # shadcn/ui 配置
│       ├── site.config.json          # 站点配置
│       ├── template_config.json      # 模板配置
│       ├── public/                   # favicon、robots 等静态资源
│       ├── prerender/                # 博客预渲染与站点地图脚本
│       └── src/
│           ├── App.tsx               # 应用根组件与路由入口
│           ├── main.tsx              # 前端启动入口
│           ├── pages/                # 首页、资源、项目、认证与博客页面
│           ├── components/
│           │   ├── layout/           # 页面布局与侧边栏
│           │   ├── chat/             # 对话消息组件
│           │   ├── preview/          # 应用预览组件
│           │   ├── blog/             # 博客布局与 Markdown 渲染
│           │   └── ui/               # shadcn/ui 通用组件
│           ├── contexts/              # 认证上下文
│           ├── hooks/                 # 移动端与 Toast Hooks
│           └── lib/                   # Supabase、智能体、博客、演示数据与工具函数
├── docs/
│   ├── system-design.md              # 系统设计与技术决策
│   ├── data-model.md                 # 数据模型、RLS 与建表规划
│   ├── deployment.md                 # 部署、环境变量与验收说明
│   └── usage.md                      # 使用指南与常见问题
└── uploads/                           # 需求参考截图
```

# File Description Inventory

| 文件或目录 | 类型 | 用途 |
|---|---|---|
| `README.md` | Markdown | 项目介绍、开发、部署和文档索引 |
| `app/frontend/package.json` | JSON | 前端依赖、开发和构建脚本 |
| `app/frontend/src/pages/` | TSX | 首页、资源、项目、认证和博客页面 |
| `app/frontend/src/components/layout/` | TSX | 主布局和侧边栏 |
| `app/frontend/src/components/chat/` | TSX | 智能体消息展示 |
| `app/frontend/src/components/preview/` | TSX | 应用实时预览 |
| `app/frontend/src/components/ui/` | TSX | 可复用 UI 组件集合 |
| `app/frontend/src/lib/agent.ts` | TypeScript | 智能体交互及演示响应 |
| `app/frontend/src/lib/supabase.ts` | TypeScript | Supabase 客户端配置 |
| `app/frontend/src/lib/demo-apps.ts` | TypeScript | 演示应用、资源和项目数据 |
| `app/frontend/src/contexts/AuthContext.tsx` | TSX | 用户认证状态管理 |
| `app/frontend/prerender/` | JavaScript | 博客页面预渲染与站点地图 |
| `app/backend/README.md` | Markdown | 后端接入说明 |
| `app/backend/skills_docs/` | Markdown | Supabase Edge Functions 与编码规范 |
| `docs/*.md` | Markdown | 系统设计、数据模型、部署和使用文档 |
| `uploads/*.png` | PNG | 产品需求及界面参考资料 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18 + Vite | 单页应用开发与构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS | 原子化样式 |
| UI 组件 | shadcn/ui、Radix UI | 可复用交互组件 |
| 路由 | React Router | 页面导航 |
| 状态与数据 | React Context、演示数据、Supabase | 认证、项目和资源数据 |
| 后端平台 | Supabase | 认证、PostgreSQL 数据库、RLS 与 Edge Functions |
| 智能体 | Agent 抽象层、流式交互 | 对话计划、代码生成和预览联动 |
| 内容处理 | Markdown、博客预渲染脚本 | 博客文章展示与静态生成 |
| 代码规范 | ESLint、TypeScript strict | 静态检查与类型校验 |
| 外部连接 | MCP | 远程工具与工作区连接 |
| 交付 | GitHub main 分支 | 代码版本管理与推送 |

当前后端尚未提供完整业务源码，数据库实体、RLS 策略和 Edge Function 接入方案记录在 `docs/` 与 `app/backend/skills_docs/` 中。前端在未配置 Supabase 时使用演示模式回退。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

如项目环境未安装 pnpm，也可使用 npm 安装：

```bash
npm install
```

## 本地开发

```bash
pnpm dev
```

## 类型检查与代码检查

```bash
pnpm lint
pnpm build
```

## 生产预览

```bash
pnpm preview
```

## 环境变量

在 `app/frontend` 中配置 Supabase 与智能体相关环境变量。具体变量名称、可选项和部署要求参见：

- `README.md`
- `docs/deployment.md`
- `app/backend/README.md`

未配置 Supabase 时，认证、资源、项目和智能体功能使用演示模式；配置完成后再切换到真实数据流程。

## Supabase 接入

1. 创建或关联 Supabase 项目。
2. 按 `docs/data-model.md` 创建 profiles、spaces、projects、conversations、messages、community_apps 和 templates 等实体。
3. 配置 RLS 策略和种子数据。
4. 设置前端环境变量。
5. 执行 lint、build 和页面级验收。
6. 在 App Viewer 中使用 Publish 完成用户侧发布。

## 交付状态

- 前端 lint 与 build 已通过。
- 首页、侧边栏、资源页和我的项目页已完成页面级走查。
- GitHub main 分支已完成分批提交和推送。
- 平台发布 MCP 工具不可用，发布链接需通过 App Viewer 的 Publish 操作生成。
- Supabase 关联和真实数据切换仍是后续接入事项。
