# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，由智能体生成可预览、可编辑、可保存和可发布的 HTML/JavaScript 应用，并支持会话恢复、项目管理、演示模式、版本管理及会话归档。

当前产品流程为“首页输入 → 会话详情开发 → 应用预览与发布”：

- 首页创建会话，历史会话可恢复消息和关联项目。
- 详情页提供对话、计划、Agent 输出、应用预览、源码、版本管理和 Publish。
- 支持基于上一轮 `app_html` 的连续增量修改。
- 项目支持版本历史、切换预览、回滚、在线 HTML 编辑和导出。
- 不活跃会话可按规则归档，不删除会话、消息、项目或版本数据。
- 归档仅作用于指定用户账户：按 `updated_at` 保留最近 5 条会话及全部收藏会话，其余标记为 `archived`。
- 详情页左侧对话区、状态卡、源码查看器与相关菜单统一为白色浅色体系（T27）。
- 真实 AI 网关当前仍因上游账户余额不足返回 HTTP 402；用户可显式使用演示模式验证完整流程。

# Project Module Description

## 工作区、认证与会话

- 支持邮箱注册、登录、退出和工作区切换。
- 注册时通过数据库触发器幂等创建 profile 与默认工作区。
- Supabase Auth、PostgreSQL 和 RLS 隔离用户、工作区、会话、消息及项目数据。
- 侧边栏支持折叠、窄屏布局、收藏、重命名和删除会话。
- 历史会话可进入详情页恢复消息、项目和版本。

## 会话归档

- `conversations.status` 默认值为 `active`，归档状态为 `archived`。
- 侧边栏最近会话和详情页历史查询统一过滤 `status='active'`。
- 旧归档会话直达时显示“该会话已归档”并返回首页。
- 归档为一次性数据整理，不提供归档管理 UI。
- 归档不删除任何 `conversations`、`messages`、`projects` 或版本数据。
- 当前已对指定真实用户账户执行清理：保留最近 5 条会话，归档 1 条最早会话；其他账户未受影响。
- 如需恢复，可在数据库层将对应会话的 `status` 改回 `active`。

## Chat 输入区

- 首页与详情页共用 `ChatComposer`。
- 支持文本、附件、语音转写、MCP、主题、构建模式和目标模式。
- `+` 菜单提供附件、MCP、视频、深度研究和竞赛模式入口。
- 输入 `#` 可引用上传内容、AI、密钥和项目。
- 支持过滤、键盘导航、Escape 关闭、点击外部区域关闭及中文输入法组合输入。
- 用户消息先更新界面，再写入数据库。

## 会话详情与应用查看

- 详情页采用顶部工具栏、左侧 Chat、右侧 App Viewer 的布局。
- 支持 SSE 流式生成、停止生成、错误恢复、重新生成和历史消息加载。
- App Viewer 支持 iframe 预览、源码查看、在线编辑、版本面板和 Publish。
- Preview 与源码页签同步当前查看的产物。
- 归档会话旧链接直达时显示归档提示，不影响活跃会话。
- 项目修改后需要重新 Publish。

## 增量修改

- 首次生成使用普通生成流程。
- 已有项目继续修改时，客户端传递 `previousHtml`。
- Edge Function 将上一轮 HTML 注入修改上下文。
- 连续多轮修改基于上一轮已落库产物。
- 每轮结果更新项目当前 HTML、Preview、源码和版本历史。

## 项目版本管理

- `project_versions` 保存版本号、来源、创建时间和 `app_html` 快照。
- 版本来源包括 `generation`、`edit` 和 `rollback`。
- 支持版本历史、切换查看、回滚、在线编辑保存和 HTML 导出。
- 回滚创建新的 `rollback` 版本，不删除旧版本。
- 刷新后通过服务端查询恢复项目版本。
- 版本加载支持请求超时、真实取消和重试；版本面板加载期间显示明确加载状态，避免误显示为空。

## 界面配色（浅色体系，T27）

- 详情页左侧对话栏使用 `bg-white` 白底配深色文字，消息、计划卡与代码进度均在浅色底上渲染。
- 状态徽标、执行计划卡、生成失败卡、空态、演示入口与消息队列面板统一使用浅色语义变量。
- 生成失败卡采用浅红底（`bg-rose-50`）加深红文字，保证对比度与可读性。
- App Viewer 源码查看器与 HTML 编辑器为白底深色字（`bg-white text-slate-800`）。
- `#` 引用菜单与 `+` 号工具菜单统一 `bg-popover` / `border-border` / `text-popover-foreground` / `text-muted-foreground` / `hover:bg-muted`，清理深色残留。
- 有意保留的深色元素：用户消息气泡的深色底浅色字、主题预览卡的 `bg-white/85` 装饰条，属设计对比而非反色残留。
- 走查以 `getComputedStyle` 实测背景与文字亮度判定（浅色 alpha>0.5 且亮度>160，深色文字亮度<120），不依赖类名匹配。

## 主题与构建模式

- 主题按钮使用 `Palette` 图标，默认不选择主题。
- 未选择主题时不注入主题提示词，并使用中性色板作为预览回退。
- 明确选择主题后才影响生成和预览，刷新后保留选择。
- 构建类型和目标模式展示名称，菜单选中项的对勾位于右侧。

## AI 生成管线

Edge Function 按以下阶段处理请求：

1. 意图识别。
2. 布局规划。
3. HTML/JavaScript 代码生成。
4. 产物清理与持久化。
5. 项目版本写入。

通过 `plan`、`message`、`step`、`app`、`done` 和 `error` SSE 事件传递生成过程。

模型与重试链路：

1. 瞬时失败时进行同模型重试。
2. 仍然失败时按顺序切换备用模型。
3. SSE 已开始输出后不切换模型。
4. 客户端对连接失败、5xx 和可重试的 429 最多执行三次指数退避重试。
5. HTTP 402 直接提示余额不足，不进行无意义重试。
6. 所有模型失败时返回明确错误，不伪造成功状态。

当前模型配置：

- 主模型：`deepseek-v4-flash`
- 备用模型：`gpt-5.4`
- 备用模型：`gemini-3.1-pro-preview`

## 失败处理与演示模式

- 生成失败卡提供重新生成入口。
- HTTP 402 显示 AI 账户余额不足提示。
- 用户可主动选择演示模式生成本地演示应用。
- 演示应用正常落库，并在消息 metadata、项目字段和 App Viewer 中显示演示标识。
- 刷新历史详情后恢复演示状态。
- 演示模式不会静默替代真实 AI。

## 语音、模板与项目

- 音频通过 `app_atoms_transcribe_audio` Edge Function 转换为文本。
- 模板支持占位字段填写、体验、克隆和魔改。
- 模板输入按 HTML/JavaScript 上下文转义。
- 我的项目展示真实项目，支持收藏、预览、版本查看和历史回放。
- 项目通过会话关联，在详情页自动恢复。

## 博客与内容

- 支持博客索引、文章详情和 Markdown 渲染。
- 提供博客预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、运行、部署和验证说明
├── PROJECT_NOTES.md                           # 实现思路、架构设计、完成度和规划
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # AI 生成、增量修改、错误分类和 SSE
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/                        # 数据库结构、版本、归档和 RLS
│   │   │   └── 007_conversation_archive_status.sql # 会话状态字段迁移
│   │   └── scripts/
│   │       ├── t26_archive_conversations.sql  # 一次性会话归档脚本
│   │       └── t26_probe.sql                  # 账户与会话分布探针
│   └── frontend/
│       ├── package.json                       # 前端依赖和脚本
│       ├── vite.config.ts                     # Vite 配置
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── *-regression.mjs                  # 浏览器、工作区和功能回归脚本
│       ├── t23-demo-mode-walkthrough.mjs     # 演示模式走查
│       ├── t25-versioning-walkthrough.mjs    # 增量修改与版本管理走查
│       ├── t26-archive-walkthrough.mjs       # 会话归档与前端过滤走查
│       ├── t27-light-theme-walkthrough.mjs   # 浅色体系亮度实测走查
│       ├── tools/                             # 本地登录会话辅助工具
│       ├── public/                            # 静态资源
│       ├── prerender/                         # 博客预渲染和站点地图脚本
│       └── src/
│           ├── App.tsx                        # 根组件与路由布局
│           ├── main.tsx                       # 前端启动入口
│           ├── pages/
│           │   ├── HomePage.tsx               # 首页输入和会话创建
│           │   └── ChatDetailPage.tsx         # 详情页、生成、版本和归档守卫
│           ├── components/                    # 布局、聊天、预览、版本和 UI
│           ├── contexts/                      # 认证和用户数据上下文
│           ├── hooks/                         # 通用 Hooks
│           └── lib/
│               ├── agent.ts                   # Edge Function 调用和 SSE 解析
│               ├── supabase.ts                # 会话、项目、版本和归档查询
│               └── demo-apps.ts               # 演示应用和主题资源
├── docs/                                      # 系统设计、数据模型、部署和使用文档
├── assets/                                    # 测试音频等资源
├── uploads/                                   # 输入区和布局参考资源
└── .atoms/
    ├── ATOMS.md                              # 项目约束、决策与上下文
    └── PROGRESS.md                            # T1-T26 工作状态与进度日志
```

临时截图、诊断 JSON 和 `.tmp-*` 文件仅用于验证，不属于产品运行所需资源。

# File Description Inventory

| 文件或目录 | 用途 |
|---|---|
| `README.md` | 项目功能、运行、部署、归档规则和验证说明 |
| `PROJECT_NOTES.md` | 产品取舍、技术架构、任务完成度和后续规划 |
| `app/frontend/package.json` | 前端依赖、脚本及 Playwright 配置 |
| `app/frontend/src/App.tsx` | 根路由及页面布局切换 |
| `app/frontend/src/pages/HomePage.tsx` | 首页输入、主题状态、项目查询和会话创建 |
| `app/frontend/src/pages/ChatDetailPage.tsx` | 详情布局、生成、版本管理和归档会话处理 |
| `app/frontend/src/components/chat/ChatComposer.tsx` | 输入区、菜单、引用、主题和发送控制 |
| `app/frontend/src/components/chat/ChatMessage.tsx` | 消息、计划卡和演示标识展示 |
| `app/frontend/src/components/layout/Sidebar.tsx` | 工作区、活跃历史会话和会话操作 |
| `app/frontend/src/lib/agent.ts` | Edge Function 调用、SSE 解析、错误透传和客户端重试 |
| `app/frontend/src/lib/supabase.ts` | 会话过滤、项目、消息和版本查询 |
| `app/frontend/src/lib/demo-apps.ts` | 演示应用、主题色板和演示资源 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | AI 管线、增量上下文、模型降级、错误分类和 SSE |
| `app/backend/functions/app_atoms_transcribe_audio/index.ts` | 音频转文字 |
| `app/backend/migrations/006_project_versions_and_metadata.sql` | 项目版本和演示元数据结构 |
| `app/backend/migrations/007_conversation_archive_status.sql` | `conversations.status` 字段、默认值和归档约束 |
| `app/backend/scripts/t26_archive_conversations.sql` | 按保留规则批量归档指定账户会话 |
| `app/backend/scripts/t26_probe.sql` | 账户识别、会话数量和归档前探测 |
| `app/frontend/t26-archive-walkthrough.mjs` | 归档过滤、旧链接处理和数据保留验证 |
| `app/frontend/t27-light-theme-walkthrough.mjs` | 详情页浅色体系背景与文字亮度实测、菜单与源码区一致性验证 |
| `app/frontend/t25-versioning-walkthrough.mjs` | 增量修改、版本切换、回滚、编辑、导出和刷新验证 |
| `app/frontend/t23-demo-mode-walkthrough.mjs` | 演示模式生成、落库和刷新回放验证 |
| `docs/data-model.md` | 数据表、字段、关联、状态和 RLS 说明 |
| `docs/deployment.md` | 部署、模型配置、错误分类、归档和回归说明 |
| `.atoms/PROGRESS.md` | T26 任务、归档结果、回归和发布记录 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18、Vite | 单页应用和页面构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS、CSS | 页面、响应式样式和布局 |
| UI 组件 | shadcn/ui、Radix UI | 菜单、Popover、选择器和交互组件 |
| 图标 | lucide-react | 导航、主题和操作图标 |
| 路由 | React Router | 首页与会话详情导航 |
| 状态与数据 | React Context、Supabase | 认证、会话、消息、项目和版本 |
| 后端平台 | Supabase Auth、PostgreSQL、RLS、Edge Functions | 认证、数据隔离和服务端逻辑 |
| 智能体 | DeepSeek、GPT、Gemini、AI 网关、SSE | 规划、代码生成、增量修改和流式传输 |
| Edge Function | Deno、TypeScript | AI 调用、重试、错误分类和产物清理 |
| 语音转写 | `scribe_v2`、AI 网关 | 音频转文字 |
| 外部连接 | MCP | 远程工具和工作区连接 |
| 内容处理 | Markdown、预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright 1.52.0、Node、Python | 浏览器回归、诊断和 E2E |
| 代码规范 | ESLint、TypeScript strict | 静态检查和类型校验 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。演示模式使用本地演示资源。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用：

```bash
npm install
```

Playwright 使用项目锁定版本 1.52.0。

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置，不要提交 `.env.local`。

在后端配置 AI 网关和密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

## 初始化数据库

按迁移顺序执行：

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
python scripts/run_sql.py migrations/004_user_init_trigger.sql
python scripts/run_sql.py migrations/005_conversation_actions.sql
python scripts/run_sql.py migrations/006_conversation_project.sql
python scripts/run_sql.py migrations/006_project_versions_and_metadata.sql
python scripts/run_sql.py migrations/007_conversation_archive_status.sql
```

## 执行一次性归档

归档前确认目标账户和保留集合，再执行：

```bash
cd app/backend
python scripts/t26_archive_conversations.sql
```

该脚本仅更新目标账户的 `conversations.status`，不会删除消息、项目或版本。恢复归档会话时，将目标记录的状态改回 `active`。

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

确认应用生成和音频转写函数已发布，并配置服务端 Secrets。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

未配置真实 AI 时，可使用用户主动触发的演示模式。真实 AI 余额不足时会明确返回 402，不会自动切换到演示模式。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
pnpm preview
```

当前质量门禁：

- `pnpm lint`：0 个错误。
- Vite build：成功。
- 博客首页和博客路由支持预渲染。
- T13、T22、T23、T25、T26、T27 及既有回归脚本已覆盖核心流程。

## 回归验证

```bash
cd app/frontend
node t27-light-theme-walkthrough.mjs
node t26-archive-walkthrough.mjs
node t25-versioning-walkthrough.mjs
node t23-demo-mode-walkthrough.mjs
node t22-theme-icon-walkthrough.mjs
node t13-chat-detail-regression.mjs
node t10-sidebar-actions-regression.mjs
node t9-agent-quality-regression.mjs
node t8-input-regression.mjs
node t7-workspace-regression.mjs
node browser-regression.mjs
```

T27 验证结果：

- T27：17/17 通过（`getComputedStyle` 实测背景与文字亮度，覆盖对话栏、状态卡、失败卡、空态、演示入口、队列面板、源码查看器、HTML 编辑器、`#` 引用菜单、`+` 号菜单与刷新回放）。
- T26：14/14 通过。
- T25：26/26 通过。
- T23：14/14 通过。
- T22：16/16 通过。
- T13：17/17 通过。
- lint：0 个错误。
- build：成功。
- 服务端确认消息未删除、项目和版本未改变、其他账户未归档。

## 使用流程

1. 注册或登录账户。
2. 确认 profile 和默认工作区已创建。
3. 在首页输入应用需求，可添加附件、语音或 MCP。
4. 可选选择构建类型和主题后提交。
5. 在详情页查看计划、流式输出和消息历史。
6. 在右侧预览应用或查看源码。
7. 基于当前项目提交增量修改。
8. 在版本面板切换历史版本或执行回滚。
9. 使用在线编辑器修改 HTML，保存后同步 Preview。
10. 导出当前版本 HTML。
11. 生成失败或余额不足时，按提示使用演示模式。
12. 刷新历史详情，确认项目、消息和演示标识恢复。
13. 修改应用后重新 Publish。
14. 归档会话不会出现在活跃历史列表；旧链接访问时显示归档提示。
