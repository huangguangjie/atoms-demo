# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，在工作区中创建会话，由智能体识别意图、制定布局计划、流式生成应用代码，并在 App Viewer 中实时预览、保存和发布应用。

平台支持账户认证、多个工作区、会话隔离、AI 代码生成、语音转写、附件、MCP 连接、资源发现、模板占位符、项目管理和博客内容。当前版本完成 T9 智能体生成质量优化：针对复杂或模糊需求增加意图识别与布局规划，强化 HTML/CSS 生成约束，并清理模型输出中的 Markdown 和代码围栏，改善生成应用的布局稳定性与可读性。

新用户注册时由数据库触发器自动创建 profile 和默认工作区，并通过安全的哈希处理生成头像颜色，避免负数取模导致注册失败。工作区继续作为会话组织单元，RLS 保证用户数据隔离。

# Project Module Description

## 应用布局与导航

- 左侧可折叠侧边栏包含工作区选择、首页、资源、我的项目和最近会话。
- 工作区选择器支持查看、切换和创建工作区。
- 最近会话仅显示当前工作区的会话。
- 侧边栏和首页均提供新建会话入口。
- 提供社群、积分、用户信息、设置和通知入口。
- 未登录时显示认证入口，主内容区域根据路由切换页面。

## 认证与工作区

- 支持邮箱注册、登录和退出。
- 数据库触发器幂等创建 `profiles` 记录和默认工作区。
- 默认工作区使用唯一性约束避免并发重复创建。
- 头像颜色生成使用非负哈希值，避免 `avatar_color` 非空约束异常。
- 登录后加载用户工作区并自动选择可用工作区。
- 工作区选择器支持命名创建，创建成功后自动切换。
- Supabase RLS 保证用户之间的数据隔离。

## 首页智能体对话

- 支持提示词、附件、主题、`#` 引用、构建/目标模式、语音输入及发送/停止。
- 新会话始终归属当前工作区。
- 切换会话时清理生成状态、消息和预览。
- 语音优先通过 `app_atoms_transcribe_audio` 服务端转写，失败时回退浏览器原生识别。
- 支持远程 MCP 服务配置、连接状态展示和持久化。
- 真实流程为创建会话、保存用户消息、SSE 流式生成、保存助手消息、预览应用和项目落库。
- Supabase 已配置时，真实请求失败会透出具体错误，不静默回退演示流程。

## 智能体意图识别与布局规划

- `app_atoms_agent_generate` 先识别用户需求中的应用类型、功能范围、内容分区和核心场景。
- 对模糊或过短的提示词注入合理默认假设，减少模型盲猜。
- 独立布局规划阶段输出：
  - 页面区域及层级结构；
  - 页头、侧栏、主区和页脚的取舍；
  - Flex/Grid 布局策略；
  - 间距体系、字号层级和配色规范；
  - 关键交互和响应式策略。
- 计划结果继续通过 `plan` 事件发送并在前端计划卡片中展示。
- 代码生成系统提示词强制要求：
  - 使用 Flex/Grid 等现代 CSS 布局；
  - 禁止无约束的绝对定位；
  - 统一间距、圆角和视觉层级；
  - 支持移动端和响应式断点；
  - 输出纯 HTML，不包含 Markdown、代码围栏或额外说明。
- 生成完成后对 HTML 进行清理，剥离代码围栏、前后置说明和其他杂文。
- 保持原有 `plan`、`message`、`step`、`app`、`done`、`error` SSE 事件协议不变。
- 使用 140 秒软超时预算、`max_tokens` 16000 和截断自动重试机制。

## 工作区与会话

- `conversations.space_id` 表示会话所属工作区。
- 切换工作区后仅显示当前工作区的最近会话。
- 支持在当前工作区中创建多个独立会话。
- 会话消息按会话归属加载，RLS 限制用户访问范围。
- 刷新或重新登录后恢复工作区和会话列表。

## App Viewer 与发布

- 对话区与应用预览区并列展示。
- 支持 iframe 应用预览和源代码查看。
- 支持计划、代码、应用及完成事件展示。
- 生成 HTML 保存到 `projects.app_html`，支持项目预览回放。
- 通过 Publish 生成发布版本；修改后的应用需要重新发布。

## 资源中心与模板

- “发现”展示社区应用并支持分类过滤。
- “模板”展示可复用应用方案。
- 模板支持查看和填写占位字段。
- 未填写的占位字段保留默认内容。
- 占位值注入时分别处理 HTML 和 JavaScript 上下文转义。
- 支持体验、克隆、魔改和使用模板等真实后端操作。
- 未登录执行受保护操作时显示认证弹窗。

## 我的项目

- 展示当前用户的真实项目列表。
- 支持全部项目和已收藏项目 Tab。
- 支持收藏、取消收藏和应用预览回放。
- 生成的 HTML 可在项目页重新查看。
- 查询或更新失败时展示真实错误。

## 后端数据与权限

- 提供 `profiles`、`spaces`、`projects`、`conversations`、`messages`、`community_apps` 和 `templates` 业务表。
- `projects.app_html` 保存生成应用 HTML。
- `templates.placeholders` 使用 JSONB 保存模板字段。
- 用户相关表关联 `auth.users` 并启用 RLS。
- `004_user_init_trigger.sql` 注册时初始化 profile 和默认工作区，并安全生成头像颜色。
- 社区应用和模板作为公共数据提供访问。

## 博客与内容

- 支持博客索引、文章详情和 Markdown 内容渲染。
- 提供博客预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、开发和部署入口
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # 意图识别、布局规划、代码生成和 HTML 清理
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/
│   │   │   ├── 001_initial_schema.sql         # 业务表、RLS 和种子数据
│   │   │   ├── 002_add_app_html.sql           # projects.app_html 字段迁移
│   │   │   ├── 002_template_placeholders.sql  # templates 占位字段迁移
│   │   │   ├── 003_space_default_unique.sql   # 默认工作区唯一性约束
│   │   │   └── 004_user_init_trigger.sql      # 注册初始化 profile 和默认工作区
│   │   └── scripts/
│   │       ├── deploy_function.py             # 部署 Edge Function
│   │       ├── e2e_verify.py                  # 认证、数据和 RLS 验证
│   │       ├── run_sql.py                     # 执行 Supabase SQL
│   │       ├── set_secrets.py                 # 配置 Edge Function Secrets
│   │       ├── test_gateway.py                # AI 网关流式调用测试
│   │       ├── test_transcribe.py             # 音频转写链路测试
│   │       ├── test_agent_e2e.py              # AI 生成端到端测试
│   │       ├── test_t9_baseline.py            # 五类提示词生成质量门禁
│   │       ├── debug_conversation.py          # 会话链路调试
│   │       ├── debug_function_logs.py         # Edge Function 日志调试
│   │       ├── verify_persisted.py            # 持久化数据核验
│   │       └── .t9_out/                       # T9 基线生成的 HTML 和计划输出
│   └── frontend/
│       ├── package.json                        # 前端依赖与脚本
│       ├── vite.config.ts                     # Vite 配置
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── browser-regression.mjs             # 浏览器回归测试
│       ├── t7-workspace-regression.mjs        # 工作区与会话回归测试
│       ├── t8-input-regression.mjs            # 输入区控件回归测试
│       ├── t9-agent-quality-regression.mjs    # 智能体生成全链路质量回归
│       ├── tools/
│       │   └── login-session-server.py        # 本地登录会话辅助服务
│       ├── public/                            # 静态资源
│       ├── prerender/                         # 博客预渲染和站点地图脚本
│       └── src/
│           ├── App.tsx                        # 应用根组件与路由入口
│           ├── main.tsx                       # 前端启动入口
│           ├── pages/                          # 首页、资源、项目和博客页面
│           ├── components/
│           │   ├── auth/                       # 登录和注册弹窗
│           │   ├── layout/                     # 布局、工作区和侧边栏
│           │   ├── workspace/                 # 工作区创建交互
│           │   ├── chat/                      # 对话消息和生成状态
│           │   ├── preview/                   # 应用预览和源码查看
│           │   ├── templates/                 # 模板占位符交互
│           │   ├── blog/                      # 博客布局与 Markdown 渲染
│           │   └── ui/                        # 通用 UI 组件
│           ├── contexts/                      # 认证和用户数据上下文
│           ├── hooks/                          # 通用 Hooks
│           └── lib/                            # Supabase、智能体和工具函数
├── docs/
│   ├── system-design.md                       # 系统设计、智能体管线和技术决策
│   ├── data-model.md                           # 数据模型、工作区、会话与 RLS
│   ├── deployment.md                            # 部署、质量门禁和回归验证
│   └── usage.md                                 # 使用指南和智能体生成流程
├── assets/
│   └── audios/voice-test.mp3                  # 语音转写测试音频
├── uploads/
│   ├── Clipboard_Screenshot_1789741538.png    # 输入区参考截图
│   └── Clipboard_Screenshot_1789741716.png    # 生成布局问题参考截图
└── .atoms/
    ├── ATOMS.md                                # 项目约束与上下文
    └── PROGRESS.md                             # T8、T9 任务状态和进度日志
```

# File Description Inventory

| 文件或目录 | 类型 | 用途 |
|---|---|---|
| `app/frontend/src/pages/HomePage.tsx` | TSX | 首页对话、工作区会话、输入区和生成流程 |
| `app/frontend/src/lib/agent.ts` | TypeScript | Edge Function 流式调用、超时和事件处理 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | TypeScript | 意图识别、布局规划、代码生成、重试和 HTML 清理 |
| `app/backend/functions/app_atoms_transcribe_audio/index.ts` | TypeScript | 服务端音频转写 |
| `app/backend/migrations/004_user_init_trigger.sql` | SQL | 注册时创建 profile 和默认工作区，生成安全头像颜色 |
| `app/backend/scripts/test_t9_baseline.py` | Python | 五类提示词的计划、HTML、SSE 和布局质量验证 |
| `app/frontend/t8-input-regression.mjs` | JavaScript | 输入区控件、模式切换、引用、附件、语音和 MCP 回归 |
| `app/frontend/t9-agent-quality-regression.mjs` | JavaScript | 注册、生成、计划卡、预览、落库和项目回放全链路回归 |
| `app/backend/scripts/.t9_out/` | HTML/JSON | T9 基线测试生成的应用和计划样本 |
| `app/frontend/src/components/layout/Sidebar.tsx` | TSX | 工作区选择、会话列表和导航 |
| `app/frontend/src/components/preview/` | TSX | 应用 HTML 预览和源代码查看 |
| `app/frontend/src/contexts/AuthContext.tsx` | TSX | 认证、profile 初始化和工作区管理 |
| `app/backend/migrations/001_initial_schema.sql` | SQL | 初始化业务表、RLS 和公共种子数据 |
| `app/backend/scripts/` | Python/SQL | 数据库执行、部署、调试和端到端验证 |
| `docs/system-design.md` | Markdown | 智能体意图识别、布局规划和生成约束 |
| `docs/usage.md` | Markdown | 用户操作和生成流程说明 |
| `docs/deployment.md` | Markdown | 部署、质量门禁和回归脚本说明 |
| `uploads/*.png` | PNG | 输入区及生成布局参考资料 |
| `.atoms/PROGRESS.md` | Markdown | T8、T9 进度与验证记录 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18 + Vite | 单页应用开发与构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS | 原子化样式系统 |
| UI 组件 | shadcn/ui、Radix UI | 可复用交互组件 |
| 路由 | React Router | 页面导航 |
| 状态与数据 | React Context、Supabase | 认证、工作区、会话和项目管理 |
| 后端平台 | Supabase | Auth、PostgreSQL、RLS 和 Edge Functions |
| 数据库 | PostgreSQL | 业务数据和公共资源存储 |
| 数据库自动化 | PostgreSQL Trigger | 注册时初始化用户数据 |
| 智能体 | `deepseek-v4-flash`、AI 网关、SSE | 意图识别、布局规划和代码生成 |
| Edge Function | Deno/TypeScript | 服务端 AI 调用、超时、重试和 HTML 清理 |
| 语音转写 | `scribe_v2`、AI 网关 | 音频转文字 |
| 外部连接 | MCP | 远程工具和工作区连接 |
| 内容处理 | Markdown、预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright、Node、Python | 浏览器回归、质量门禁和 E2E 验证 |
| 代码规范 | ESLint、TypeScript strict | 静态检查与类型校验 |
| 交付 | GitHub main | 版本管理和代码交付 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。生成管线包含意图识别、布局规划、带硬约束的代码生成、输出清理和截断重试。当前生成质量门禁覆盖纯 HTML、Flex/Grid、响应式布局、计划完整性和 SSE 事件完整性。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用：

```bash
npm install
```

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置。不要提交 `.env.local`。

配置服务端 AI 网关和密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

详细变量要求参见 `README.md`、`docs/deployment.md` 和 `app/backend/README.md`。

## 初始化数据库

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
python scripts/run_sql.py migrations/004_user_init_trigger.sql
```

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

部署后确认应用生成和音频转写函数均已发布，并配置服务端 Secrets。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

未配置 Supabase 时使用演示资源和本地演示智能体；已配置时使用真实认证、数据和生成链路。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
pnpm preview
```

## 生成质量验证

```bash
cd app/backend
python scripts/test_t9_baseline.py
```

质量门禁覆盖游戏、工具、数据展示、内容和复合分区五类提示词，检查计划内容、纯 HTML 输出、Flex/Grid、响应式样式及事件完整性。

## 浏览器回归验证

```bash
cd app/frontend
node t8-input-regression.mjs
node t9-agent-quality-regression.mjs
node t7-workspace-regression.mjs
node browser-regression.mjs
```

T9 全链路回归覆盖注册、默认工作区、意图识别、计划卡布局规划、SSE 生成、iframe 预览、会话与项目落库、刷新恢复和项目回放，共 14 项通过。T8 输入区回归覆盖附件、语音、`#` 引用、模式切换和 MCP 操作。

## 使用流程

1. 注册或登录账户。
2. 确认系统自动创建 profile 和默认工作区。
3. 切换已有工作区或创建新的工作区。
4. 从侧边栏或首页创建当前工作区下的新会话。
5. 输入具体的应用目标、核心功能和使用场景，也可添加附件或使用语音输入。
6. 查看智能体识别结果和布局计划卡片。
7. 查看流式代码生成和实时应用预览。
8. 在资源页使用模板并填写占位字段。
9. 在我的项目中收藏、预览和回放已保存应用。
10. 在 App Viewer 中重新 Publish 以发布最新版本。
