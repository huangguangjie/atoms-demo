# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，选择工作区、构建模式和可选主题，并使用附件、语音、引用及 MCP 等能力，由智能体生成可预览、保存和发布的应用。

当前产品流程为“首页输入 → 会话详情开发 → 应用查看与发布”：

- 首页提交需求后创建会话并进入详情页。
- 历史会话进入独立详情页，不保留首页全局导航。
- 详情页左侧完整回放用户消息、计划卡和 Agent 输出，右侧自动展示关联应用。
- 会话消息和生成结果可持久化，并在刷新后恢复。
- 详情页左右区域均铺满可用视口高度，长消息可滚动，新消息保持在底部。
- 主题默认未选择；主题按钮恢复 `Palette` 图标。
- AI 网关 5xx/429 错误支持前后端退避重试，失败后保留上下文并提供“重新生成”入口。
- App Viewer 支持应用预览、源码查看和发布。

# Project Module Description

## 应用布局与导航

- 首页使用完整工作区布局，提供侧边栏、欢迎状态和输入区。
- 详情页使用顶部工具栏、左侧 Chat 区和右侧 App Viewer 的三段式布局。
- 详情页通过完整高度链铺满顶栏以下可用空间，消除底部白色断层。
- 历史会话点击后直接进入详情页，并完整加载消息和关联应用。
- 侧边栏支持折叠、窄屏布局、收藏、重命名和删除会话。
- 详情页保留返回首页和历史会话切换入口，不显示首页、资源和项目等全局菜单。

## 认证与工作区

- 支持邮箱注册、登录和退出。
- 注册时通过数据库触发器幂等创建 profile 和默认工作区。
- Supabase Auth 与 RLS 隔离用户、工作区、会话、消息和项目数据。
- 登录后按工作区筛选会话。
- 未登录时避免执行无效项目查询，并显示认证提示。
- 测试登录注入会等待 Supabase 会话建立后再跳转，避免清除访问令牌 hash。

## 首页输入与对话详情

- 首页负责欢迎状态、需求输入、主题选择、会话创建和跳转。
- 详情页负责历史消息加载、流式生成、停止生成、错误恢复和应用结果查看。
- 输入区支持文本、附件、`#` 引用、MCP、语音转写、主题、构建模式、发送和停止。
- 历史详情按时间顺序展示用户消息、计划卡、Agent 输出和最终结果。
- 用户消息会先更新界面，再写入 `messages`，避免刷新后丢失。
- 会话通过 `conversation_id` 与项目关联，详情页自动恢复关联应用。
- 无关联应用或生成失败时，右侧显示明确空状态，而不是空白区域。

## 输入区与引用菜单

- `+` 面板提供附件、MCP、视频、深度研究和竞赛模式等入口。
- 输入框键入 `#` 时显示上传、AI、密钥和项目引用菜单。
- 支持文本过滤、键盘导航、Escape 关闭和点击外部区域关闭。
- 项目引用沿用现有注入逻辑并生成引用 chip。
- 菜单不干扰中文输入法组合输入。
- 首页与详情页共用 `ChatComposer`。

## 主题与构建模式

- 主题按钮显示 `Palette` 图标，并与其他输入工具保持一致的尺寸、颜色和 hover 状态。
- 主题默认未选择，不自动高亮或恢复预设主题。
- 未选择主题时不注入主题提示词，并使用中性色板作为预览回退。
- 用户明确选择主题后才会高亮、持久化并影响生成和预览。
- 刷新后保留用户已选择的主题。
- 构建类型和目标模式仍只显示名称，不显示图标。
- 菜单选中项的对勾位于行右侧。

## 智能体生成链路

- Edge Function 按意图识别、布局规划、代码约束生成和产物清理处理请求。
- 通过 `plan`、`message`、`step`、`app`、`done` 和 `error` SSE 事件传递结果。
- 生成代码强调 Flex/Grid、响应式断点和受控定位。
- 服务端对上游 5xx 进行退避重试；客户端对瞬时连接失败、5xx 和 429 进行最多三次指数退避重试。
- 最终失败时保留用户输入和会话上下文，展示可操作错误卡及“重新生成”入口。
- 生成失败不会伪造成功状态，错误信息保留可识别的上游状态。
- 重新生成不会重复落库原始用户消息。
- 生成输出会清理 Markdown、代码围栏和额外说明，并限制超时、输出长度和重试次数。

## 应用查看器与发布

- 详情页右侧自动加载会话关联项目并显示 iframe 预览。
- 支持应用源码查看。
- 生成 HTML 保存到 `projects.app_html`。
- Publish 生成发布版本；应用修改后需要重新发布。
- 无关联项目、项目尚未落库或 AI 生成失败时显示合理空状态。
- 概览、编辑器、Atoms 云、文件、增长、终端、计划器、浏览器和记事本等标签仍有部分占位实现。

## 资源、模板与项目

- 资源页展示社区应用并支持分类筛选。
- 模板支持占位字段填写、体验、克隆和魔改，并按 HTML/JavaScript 上下文转义输入。
- 我的项目展示真实项目，支持收藏、预览和历史回放。
- 项目可通过会话关联，在详情页自动恢复并展示。

## 博客与内容

- 支持博客索引、文章详情和 Markdown 渲染。
- 提供博客预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、开发与部署入口
├── PROJECT_NOTES.md                           # 架构取舍、完成度与后续规划
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # 智能体生成、重试与 HTML 清理
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/                        # 数据库结构、触发器、关联和 RLS
│   │   └── scripts/                           # 部署、SQL、调试和质量验证脚本
│   └── frontend/
│       ├── package.json                       # 前端依赖与脚本
│       ├── vite.config.ts                     # Vite 配置
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── *-regression.mjs                  # 浏览器、工作区和功能回归脚本
│       ├── t22-theme-icon-walkthrough.mjs     # 主题图标及相关链路专项走查
│       ├── tools/                             # 本地登录会话辅助工具
│       ├── public/                            # 静态资源
│       ├── prerender/                         # 博客预渲染和站点地图脚本
│       └── src/
│           ├── App.tsx                        # 根组件与路由布局
│           ├── main.tsx                       # 前端启动入口
│           ├── pages/
│           │   ├── HomePage.tsx               # 首页输入、主题和会话创建
│           │   └── ChatDetailPage.tsx         # 详情布局、历史回放和应用查看
│           ├── components/                    # 认证、布局、聊天、预览、模板、博客和 UI
│           ├── contexts/                      # 认证和用户数据上下文
│           ├── hooks/                          # 通用 Hooks
│           └── lib/                            # Supabase、智能体和演示资源封装
├── docs/                                      # 系统设计、数据模型、部署和使用文档
├── assets/                                    # 测试音频等资源
├── uploads/                                   # 输入区和布局参考资源
└── .atoms/
    ├── ATOMS.md                              # 项目约束与上下文
    └── PROGRESS.md                            # T1-T22 任务状态与进度日志
```

临时回归截图、走查结果 JSON 和其他 `.tmp-*` 文件仅用于验证，不属于产品运行所需资源。

# File Description Inventory

| 文件或目录 | 类型 | 用途 |
|---|---|---|
| `PROJECT_NOTES.md` | Markdown | 记录架构取舍、完成度和后续优先级 |
| `app/frontend/src/App.tsx` | TSX | 根路由及首页、详情页布局切换 |
| `app/frontend/src/pages/HomePage.tsx` | TSX | 首页输入、主题状态、项目查询和会话创建 |
| `app/frontend/src/pages/ChatDetailPage.tsx` | TSX | 详情布局、消息回放、错误卡、项目恢复和流式生成 |
| `app/frontend/src/components/chat/ChatComposer.tsx` | TSX | 输入区、主题图标、附件、MCP、引用菜单和发送控制 |
| `app/frontend/src/components/layout/Sidebar.tsx` | TSX | 工作区、历史会话、导航和会话操作 |
| `app/frontend/src/lib/agent.ts` | TypeScript | Edge Function 调用、SSE 解析、超时和客户端重试 |
| `app/frontend/src/lib/supabase.ts` | TypeScript | 会话、消息、项目及会话项目关联查询 |
| `app/frontend/src/lib/demo-apps.ts` | TypeScript | 演示应用、主题色板和中性回退 |
| `app/frontend/src/index.css` | CSS | 全局布局、高度链和基础样式 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | TypeScript | AI 管线、服务端重试、SSE 输出和 HTML 清理 |
| `app/backend/functions/app_atoms_transcribe_audio/index.ts` | TypeScript | 音频转文字 |
| `app/backend/migrations/006_conversation_project.sql` | SQL | 会话与项目关联结构及约束 |
| `app/frontend/src/components/ui/dropdown-menu.tsx` | TSX | 下拉菜单及右侧选中标记 |
| `app/frontend/src/components/ui/select.tsx` | TSX | 选择菜单及右侧选中标记 |
| `app/frontend/t22-theme-icon-walkthrough.mjs` | JavaScript | 主题按钮、菜单、错误恢复和窄屏链路走查 |
| `.atoms/PROGRESS.md` | Markdown | T20-T22 完成状态、验证结果和进度日志 |
| `app/backend/scripts/test_edge_prompt.py` | Python | Edge Function 及 AI 提示链路验证 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18、Vite | 单页应用和页面构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS、CSS | 页面、响应式样式和布局高度链 |
| UI 组件 | shadcn/ui、Radix UI | 菜单、Popover、选择器和交互组件 |
| 图标 | lucide-react | 导航、主题和操作图标 |
| 路由 | React Router | 首页与会话详情导航 |
| 状态与数据 | React Context、Supabase | 认证、工作区、会话、消息和项目 |
| 后端平台 | Supabase Auth、PostgreSQL、RLS、Edge Functions | 认证、数据隔离和服务端逻辑 |
| 智能体 | `deepseek-v4-flash`、AI 网关、SSE | 意图识别、规划和代码生成 |
| Edge Function | Deno、TypeScript | AI 调用、重试、超时和产物清理 |
| 语音转写 | `scribe_v2`、AI 网关 | 音频转文字 |
| 外部连接 | MCP | 远程工具和工作区连接 |
| 内容处理 | Markdown、预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright、Node、Python | 浏览器回归、质量门禁和 E2E |
| 代码规范 | ESLint、TypeScript strict | 静态检查和类型校验 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用：

```bash
npm install
```

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置，不要提交 `.env.local`。

在后端配置 AI 网关和密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

## 初始化数据库

按迁移顺序执行：

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
python scripts/run_sql.py migrations/004_user_init_trigger.sql
python scripts/run_sql.py migrations/005_conversation_actions.sql
python scripts/run_sql.py migrations/006_conversation_project.sql
```

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

确认应用生成和音频转写函数已发布，并配置服务端 Secrets。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

未配置 Supabase 时可使用演示资源和本地演示智能体；完成配置后使用真实认证、数据和 AI 生成链路。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
pnpm preview
```

## 回归验证

```bash
cd app/frontend
node t22-theme-icon-walkthrough.mjs
node t13-chat-detail-regression.mjs
node t10-sidebar-actions-regression.mjs
node t9-agent-quality-regression.mjs
node t8-input-regression.mjs
node t7-workspace-regression.mjs
node browser-regression.mjs
```

当前质量验证结果：

- lint：0 个错误。
- build：成功。
- T22 专项走查：16/16 通过。
- T7、T8、T10、T13 既有回归保持通过。
- AI 上游网关仍可能出现 403，Edge Function 会表现为 502；该问题属于外部服务故障，失败卡和重新生成流程可继续使用。

## 使用流程

1. 注册或登录账户。
2. 确认 profile 和默认工作区已创建。
3. 切换工作区或创建新工作区。
4. 在首页输入应用需求，可添加附件、语音或 MCP。
5. 如需引用，在输入框输入 `#` 并选择上传、AI、密钥或项目。
6. 可选选择构建类型和主题；未选择主题时使用中性默认处理。
7. 提交需求后进入对话详情页。
8. 在左侧查看完整计划、流式输出和消息历史。
9. 在右侧自动预览关联应用或查看源码。
10. 生成失败时使用错误卡中的“重新生成”入口。
11. 从详情页返回首页，或通过历史会话切换其他会话。
12. 修改应用后重新 Publish 以发布最新版本。
