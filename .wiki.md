# Project Summary

Atoms 是一款面向开发者的智能体驱动应用生成平台。用户通过对话描述需求，选择功能、主题和执行计划，由智能体协助生成代码应用，并以可视化网页形式预览、体验、修改和部署。平台同时提供资源发现、模板复用、项目管理、社区分享及远程 MCP 连接能力。

当前项目处于产品原型与规划阶段，已完成场景归类为 `ai_chatbot`，并确认采用分步计划执行、六大功能模块及推荐 LLM 方案。受当前环境限制，生成应用暂不采用 Next.js 模板，改用 React + Vite + shadcn/ui 等等价方案；GitHub 交付需通过 PAT 推送。

# Project Module Description

## 应用主框架

- 左右布局：左侧为功能导航，右侧为内容区域。
- 支持侧边栏展开与收起。
- 支持空间选择，默认进入当前用户的默认空间。
- 底部提供用户信息、设置和通知入口。

## 首页智能体对话

- 展示欢迎内容和用户输入区域。
- 支持提示词输入、附件选择、主题切换和 `#` 文件/位置/关键信息引用。
- 支持构建模式与目标模式切换。
- 支持语音输入、发送和停止生成。
- 支持连接远程 MCP。
- 提供“发现”“我的项目”“模板”快捷内容切换。

## 资源中心

- “发现”展示社区分享的应用，可体验、使用、克隆、修改或自行部署。
- “模板”提供可直接替换占位内容的开发方案。
- 支持发现与模板 Tab 切换。

## 我的项目

- 展示用户克隆、基于模板创建或自主创建的项目。
- 支持查看收藏项目列表。
- 后续可扩展项目预览、编辑、部署和版本管理。

## 最近对话与附加链接

- 展示最近的智能体对话记录。
- 提供“加入我们的社群”和“获取免费积分”等入口。

## 计划确认流程

- 按 `ai_chatbot` 场景生成默认功能与主题确认卡。
- 支持确认六大功能模块、分步计划执行和 LLM 推荐方案。
- 基于问答历史生成 `PlanDraft`，包含默认内容、任务拆解和里程碑，供用户审批。

# Directory Tree

```text
.
└── uploads/
    ├── Clipboard_Screenshot_1789700631.png
    ├── Clipboard_Screenshot_1789701808.png
    └── Clipboard_Screenshot_1789701865.png
```

- `uploads/Clipboard_Screenshot_1789700631.png`：产品需求或界面参考截图。
- `uploads/Clipboard_Screenshot_1789701808.png`：产品需求或界面参考截图。
- `uploads/Clipboard_Screenshot_1789701865.png`：产品需求或界面参考截图。

# File Description Inventory

| 文件 | 类型 | 用途 |
|---|---|---|
| `uploads/Clipboard_Screenshot_1789700631.png` | PNG | UI 与交互设计参考 |
| `uploads/Clipboard_Screenshot_1789701808.png` | PNG | UI 与交互设计参考 |
| `uploads/Clipboard_Screenshot_1789701865.png` | PNG | UI 与交互设计参考 |

当前目录仅包含设计参考图片，尚未提供应用源码、配置文件、数据库模型或开发文档。

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React + Vite | 应用界面与构建 |
| 语言 | TypeScript strict | 类型安全 |
| UI | Tailwind CSS + shadcn/ui | 样式与可复用组件 |
| AI 编排 | Vercel AI SDK + LangChain.js | LLM 调用与任务编排 |
| 浏览器沙箱 | `@webcontainer/api` | 浏览器端运行生成的 Node.js 应用 |
| 数据库 | PostgreSQL | 用户、项目、对话等持久化数据 |
| ORM | Prisma | 类型安全的数据访问 |
| 缓存 | Redis（ioredis） | 会话、限流与任务队列 |
| 文件存储 | MinIO（S3 兼容） | 项目文件与附件 |
| 认证 | NextAuth.js v5 | OAuth 与邮箱认证 |
| 客户端状态 | Zustand | 页面与交互状态管理 |
| 实时输出 | SSE（AI SDK 内置） | 对话流式响应 |
| 外部连接 | MCP | 远程工具与代码工作区连接 |

当前环境不支持 Next.js 项目模板，因此生成应用以 React + Vite 等价方案交付。GitHub 服务未接入当前 MCP 网关，代码交付需使用 PAT 推送。

# Usage

## 安装依赖

当前仓库尚未提供 `package.json` 或完整源码，因此暂时无法执行标准依赖安装。补充前端源码后，可使用项目声明的包管理器安装依赖，例如：

```bash
npm install
```

## 开发与构建

在源码和脚本补齐后，预期使用以下命令：

```bash
npm run dev
npm run build
npm run preview
```

## 后端与基础设施

部署完整产品前，需要准备：

- PostgreSQL 数据库及 Prisma schema。
- Redis 服务，用于会话、限流和任务队列。
- MinIO 或其他 S3 兼容对象存储。
- LLM 服务凭证与 Vercel AI SDK 配置。
- MCP 连接配置。
- OAuth、邮箱认证及 GitHub PAT 配置。

## 交付准备

当前尚未包含可测试部署链接、GitHub 代码仓库链接或开发文档。正式交付前需要补充：

- 开发与部署文档。
- 可访问的测试环境。
- GitHub 代码仓库及提交记录。
- 环境变量示例和数据库初始化说明。
