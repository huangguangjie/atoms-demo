# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言输入需求，选择工作区与会话，使用附件、语音、主题、构建模式和 MCP 等能力，由智能体生成应用代码并在 App Viewer 中预览、保存和发布。

当前产品已扩展为“输入区 + 对话详情页 + 应用结果区”的开发工作流：

- 首页提交输入后创建会话并进入 `/chat/:conversationId` 对话详情页。
- 侧边栏历史会话支持悬停操作，可收藏、重命名和删除，并实时同步数据库。
- 输入区的 `+` 面板改为深色分组面板，支持旋转展开动画，同时保留附件、`#` 引用、MCP、语音和生成控制。
- 主题面板支持搜索、选择和预览，所选主题真实传入生成链路并影响应用输出。
- 构建类型选择框改为仅显示文本，不再显示图标。
- 对话详情页采用左侧聊天开发区、右侧应用查看器布局，支持历史消息回放、流式生成和应用源码查看。

# Project Module Description

## 应用布局与导航

- 左侧侧边栏提供 Logo、工作区选择、首页、资源、我的项目和最近会话。
- 最近会话按当前工作区过滤。
- 会话项默认隐藏操作按钮，悬停时显示胶囊形省略号按钮。
- 操作菜单包含收藏、重命名和删除：
  - 收藏会更新 `conversations.is_favorite`；
  - 重命名使用输入框并写回数据库；
  - 删除需要二次确认并真实删除会话；
  - 操作完成后即时刷新列表和当前状态。
- 侧边栏支持折叠，不影响窄屏布局和会话导航。
- 点击历史会话进入对话详情页，而非继续停留在首页。

## 认证与工作区

- 支持邮箱注册、登录和退出。
- 注册时由数据库触发器幂等创建 profile 和默认工作区。
- 默认工作区通过唯一约束避免并发重复创建。
- Supabase RLS 隔离用户、工作区、会话、消息和项目数据。
- 登录后自动加载工作区，并按工作区过滤会话。

## 首页输入与对话详情

- 首页负责欢迎状态、需求输入和提交跳转。
- 提交输入后创建会话、保存用户消息，并立即进入对话详情页。
- `/chat/:conversationId` 负责历史回放、智能体生成和应用开发过程展示。
- 对话详情页左侧包含消息、计划卡、流式输出和共享输入组件。
- 输入组件支持：
  - 文本提示词；
  - 附件上传；
  - `#` 引用；
  - MCP 连接；
  - 语音转写；
  - 构建/目标模式；
  - 发送与停止生成。
- 生成中保持原有停止逻辑，空闲时显示发送操作。
- 历史会话加载消息后支持完整回放。

## 输入区 `+` 面板

- `+` 按钮展开时旋转 45 度，视觉上转换为关闭状态；收起时恢复。
- 面板采用深色 Popover，按分组展示：
  - 团队模式；
  - 附件；
  - 连接器/MCP；
  - 视频；
  - 深度研究；
  - 竞赛模式。
- 保留既有附件、`#` 引用、MCP 和生成链路，不因布局调整删除功能。
- 支持点击外部和 Escape 关闭面板。
- MCP 连接状态和徽标数量继续显示。

## 主题选择

- 主题面板采用搜索、主题列表和预览卡片布局。
- 主题选择真实更新当前生成上下文，并传入智能体生成请求。
- 主题会影响演示应用色板和生成应用视觉风格。
- 输入区的构建类型选择框仅显示名称，不再显示图标。

## 智能体意图识别与布局规划

- `app_atoms_agent_generate` 识别应用类型、功能范围、内容分区和使用场景。
- 对模糊提示词注入合理默认假设。
- 独立布局规划阶段输出页面结构、布局策略、视觉规范和响应式策略。
- 计划结果通过 `plan` SSE 事件发送并在对话详情页展示。
- 代码生成继续强制使用 Flex/Grid、响应式断点和受控定位。
- 生成结果清理 Markdown、代码围栏和额外说明。
- 保持 `plan`、`message`、`step`、`app`、`done`、`error` 事件协议。
- 使用超时预算、截断重试和最大输出限制。

## 应用查看器与发布

- 对话详情页右侧展示应用结果。
- 当前实现支持应用查看器、iframe 预览和源码查看。
- 概览、编辑器、Atoms 云、文件、增长、终端、计划器、浏览器和记事本等后续面板保留为占位或待实现状态。
- 生成 HTML 保存到 `projects.app_html`。
- Publish 生成发布版本；应用修改后需要重新发布。

## 资源、模板与项目

- 资源页展示社区应用并支持分类筛选。
- 模板支持查看、填写占位字段、体验、克隆和魔改。
- 占位值分别按 HTML 与 JavaScript 上下文转义。
- 我的项目展示真实项目，支持收藏、预览和回放。
- 未登录执行受保护操作时显示认证弹窗。

## 后端数据与权限

- 业务表包括 `profiles`、`spaces`、`projects`、`conversations`、`messages`、`community_apps` 和 `templates`。
- `conversations.is_favorite` 支持会话收藏。
- `005_conversation_actions.sql` 为收藏字段和排序建立数据库支持。
- `projects.app_html` 保存生成应用 HTML。
- 用户相关表启用 RLS；社区应用和模板作为公共数据访问。

## 博客与内容

- 支持博客索引、文章详情和 Markdown 渲染。
- 提供博客预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、开发和部署入口
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # 意图识别、布局规划、代码生成和 HTML 清理
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/
│   │   │   ├── 001_initial_schema.sql         # 初始化业务表、RLS 和种子数据
│   │   │   ├── 002_add_app_html.sql           # projects.app_html 字段
│   │   │   ├── 002_template_placeholders.sql  # 模板占位字段
│   │   │   ├── 003_space_default_unique.sql   # 默认工作区唯一约束
│   │   │   ├── 004_user_init_trigger.sql      # 注册初始化 profile 和默认工作区
│   │   │   └── 005_conversation_actions.sql   # 会话收藏字段和排序索引
│   │   └── scripts/                            # 部署、SQL、调试和质量验证脚本
│   └── frontend/
│       ├── package.json                        # 前端依赖与脚本
│       ├── vite.config.ts                     # Vite 配置
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── browser-regression.mjs             # 浏览器回归测试
│       ├── t7-workspace-regression.mjs        # 工作区与会话回归测试
│       ├── t8-input-regression.mjs            # 输入区功能回归测试
│       ├── t9-agent-quality-regression.mjs    # 智能体生成质量回归
│       ├── t10-sidebar-actions-regression.mjs # 侧边栏会话操作回归
│       ├── tools/
│       │   └── login-session-server.py        # 本地登录会话辅助服务
│       ├── public/                            # 静态资源
│       ├── prerender/                         # 博客预渲染和站点地图脚本
│       └── src/
│           ├── App.tsx                        # 根组件与路由
│           ├── main.tsx                       # 前端启动入口
│           ├── pages/
│           │   ├── HomePage.tsx               # 首页欢迎状态和输入提交
│           │   └── ChatDetailPage.tsx         # 对话详情、流式开发和应用查看
│           ├── components/
│           │   ├── auth/                      # 登录和注册弹窗
│           │   ├── layout/                    # 布局、工作区和侧边栏
│           │   ├── workspace/                 # 工作区创建交互
│           │   ├── chat/
│           │   │   ├── ChatComposer.tsx       # 复用输入区、主题和 + 面板
│           │   │   └── ...                    # 消息与生成状态组件
│           │   ├── preview/                   # 应用预览和源码查看
│           │   ├── templates/                 # 模板占位符交互
│           │   ├── blog/                      # 博客布局与 Markdown 渲染
│           │   └── ui/                        # 通用 UI 组件
│           ├── contexts/                      # 认证和用户数据上下文
│           ├── hooks/                         # 通用 Hooks
│           └── lib/
│               ├── supabase.ts                # Supabase 数据访问与会话操作
│               └── demo-apps.ts               # 演示应用和主题色板
├── docs/
│   ├── system-design.md                       # 系统设计与智能体管线
│   ├── data-model.md                          # 数据模型、工作区与 RLS
│   ├── deployment.md                          # 部署、质量门禁和回归验证
│   └── usage.md                               # 使用指南和生成流程
├── assets/
│   └── audios/voice-test.mp3                  # 语音转写测试音频
├── uploads/                                   # 输入区和布局参考截图
└── .atoms/
    ├── ATOMS.md                               # 项目约束与上下文
    └── PROGRESS.md                            # T8-T13 任务状态与进度日志
```

# File Description Inventory

| 文件或目录 | 类型 | 用途 |
|---|---|---|
| `app/frontend/src/App.tsx` | TSX | 应用路由、首页和对话详情页入口 |
| `app/frontend/src/pages/HomePage.tsx` | TSX | 首页欢迎视图、输入提交和会话创建 |
| `app/frontend/src/pages/ChatDetailPage.tsx` | TSX | 历史回放、智能体生成和应用开发详情 |
| `app/frontend/src/components/chat/ChatComposer.tsx` | TSX | 复用输入区、附件、引用、MCP、主题和 + 面板 |
| `app/frontend/src/components/layout/Sidebar.tsx` | TSX | 工作区、会话列表、悬停菜单和导航 |
| `app/frontend/src/lib/supabase.ts` | TypeScript | 认证、会话查询、收藏、重命名和删除封装 |
| `app/frontend/src/lib/demo-apps.ts` | TypeScript | 演示应用与主题色板映射 |
| `app/backend/migrations/005_conversation_actions.sql` | SQL | 增加会话收藏字段及排序索引 |
| `app/frontend/t10-sidebar-actions-regression.mjs` | JavaScript | 验证会话悬停操作、确认、持久化和刷新 |
| `app/frontend/t8-input-regression.mjs` | JavaScript | 验证输入区、附件、引用、语音、MCP 和生成链路 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | TypeScript | 意图识别、布局规划、生成、重试和 HTML 清理 |
| `app/frontend/src/lib/agent.ts` | TypeScript | Edge Function 流式调用、超时和 SSE 事件处理 |
| `app/frontend/src/components/preview/` | TSX | iframe 应用预览和源码查看 |
| `app/frontend/src/contexts/AuthContext.tsx` | TSX | 认证、profile 和工作区管理 |
| `app/backend/migrations/001_initial_schema.sql` | SQL | 初始化业务表、RLS 和公共种子数据 |
| `app/backend/scripts/` | Python/SQL | 数据库执行、部署、调试和端到端验证 |
| `docs/usage.md` | Markdown | 首页提交、详情页开发和应用发布流程 |
| `docs/deployment.md` | Markdown | 部署、质量门禁和回归脚本说明 |
| `.atoms/PROGRESS.md` | Markdown | T8-T13 任务和验证记录 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18 + Vite | 单页应用、路由和页面构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS | 页面与响应式样式 |
| UI 组件 | shadcn/ui、Radix UI | Popover、Dialog、菜单和通用交互 |
| 图标 | lucide-react | 输入区、导航和操作图标 |
| 路由 | React Router | 首页与 `/chat/:conversationId` 详情导航 |
| 状态与数据 | React Context、Supabase | 认证、工作区、会话、消息和项目 |
| 后端平台 | Supabase | Auth、PostgreSQL、RLS 和 Edge Functions |
| 数据库 | PostgreSQL | 业务数据、会话收藏和公共资源 |
| 智能体 | `deepseek-v4-flash`、AI 网关、SSE | 意图识别、布局规划和代码生成 |
| Edge Function | Deno/TypeScript | AI 调用、超时、重试和 HTML 清理 |
| 语音转写 | `scribe_v2`、AI 网关 | 音频转文字 |
| 外部连接 | MCP | 远程工具和工作区连接 |
| 内容处理 | Markdown、预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright、Node、Python | 浏览器回归、质量门禁和 E2E |
| 代码规范 | ESLint、TypeScript strict | 静态检查和类型校验 |
| 交付 | GitHub main | 版本管理和代码交付 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。生成链路包含意图识别、布局规划、约束生成、输出清理和截断重试。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用：

```bash
npm install
```

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置，不要提交 `.env.local`。

配置服务端 AI 网关和密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

## 初始化数据库

按迁移顺序执行：

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
python scripts/run_sql.py migrations/004_user_init_trigger.sql
python scripts/run_sql.py migrations/005_conversation_actions.sql
```

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

确认应用生成和音频转写函数已发布，并配置服务端 Secrets。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

未配置 Supabase 时可使用演示资源和本地演示智能体；配置后使用真实认证、数据和生成链路。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
pnpm preview
```

## 质量与浏览器回归

```bash
cd app/backend
python scripts/test_t9_baseline.py
```

```bash
cd app/frontend
node t8-input-regression.mjs
node t9-agent-quality-regression.mjs
node t10-sidebar-actions-regression.mjs
node t7-workspace-regression.mjs
node browser-regression.mjs
```

T10 回归覆盖会话收藏、重命名、删除确认、数据库持久化和列表刷新，共 20 项通过。前端 lint 和 build 已通过；T13 详情页代码已完成类型与构建验证。

## 使用流程

1. 注册或登录账户。
2. 确认系统创建 profile 和默认工作区。
3. 切换工作区或创建新工作区。
4. 在首页输入应用需求，可添加附件、引用、语音或 MCP。
5. 选择构建类型和主题；构建类型仅显示文本名称。
6. 提交输入后自动创建会话并进入对话详情页。
7. 在左侧查看计划卡、流式代码生成和消息回放。
8. 在右侧应用查看器中预览 iframe 或查看源码。
9. 在侧边栏悬停历史会话，使用收藏、重命名或删除操作。
10. 在资源和项目页面使用模板、收藏和回放应用。
11. 在 App Viewer 中重新 Publish 以发布最新版本。
