# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，在工作区中创建和管理多个独立会话，经计划确认、AI 流式代码生成和实时预览后，将应用保存、复用并发布。

平台已接入 Supabase 真实数据、认证、RLS、AI 生成、语音转写、资源发现、模板占位符、项目管理和博客内容能力。当前版本重点完善了认证错误透传、未登录操作守卫、会话与消息持久化、AI 生成超时与流中断处理，以及工作区与多会话的数据关系。

工作区是 Chat 的组织单元。每个用户可拥有多个工作区，每个工作区下可创建多个会话；侧边栏会根据当前工作区筛选最近会话。登录后若没有工作区，系统会自动创建默认工作区，并支持手动创建其他工作区。

# Project Module Description

## 应用布局与导航

- 左侧可折叠侧边栏包含工作区选择、首页、资源、我的项目和最近会话。
- 最近会话按当前工作区筛选。
- 支持新建工作区和新建会话。
- 提供社群、积分、用户信息、设置和通知入口。
- 未登录时显示认证入口，主内容区域根据路由切换页面。

## 认证与工作区

- 支持邮箱注册、登录和退出。
- 新用户登录后幂等初始化 profile 与默认工作区。
- 空工作区时自动创建默认工作区，避免工作区选择器为空。
- 工作区选择器支持查看当前用户的工作区、切换工作区和创建工作区。
- 认证初始化支持并发保护，避免重复插入 profile 或默认工作区。
- Supabase RLS 保证用户数据隔离。

## 首页智能体对话

- 支持提示词、附件、主题切换、`#` 引用、构建/目标模式、语音输入及发送/停止。
- 未登录发起对话时先打开认证弹窗，不使用伪造的 `demo-user`。
- 新会话始终归属当前工作区。
- 语音录音优先上传至 `app_atoms_transcribe_audio`，通过平台 AI 网关使用 `scribe_v2` 转写。
- 转写失败或浏览器不支持服务端链路时，自动回退浏览器原生识别。
- 支持远程 MCP 服务配置、连接状态展示和持久化。
- 真实生成流程为创建会话 → 保存用户消息 → SSE 流式代码生成 → 保存助手消息 → 应用预览 → 项目落库。
- Supabase 已配置时，真实数据或 AI 请求失败会透出具体错误，不静默回退演示流程。
- AI 生成具有软超时、流中断检测和不完整输出重试机制。
- Edge Function `app_atoms_agent_generate` 负责调用 AI 网关和输出生成事件。

## 工作区与会话

- `conversations.space_id` 表示会话所属工作区。
- 侧边栏切换工作区后，仅显示当前工作区的最近会话。
- 支持在当前工作区下新建多个独立会话。
- 未选择工作区时，先引导创建或选择工作区，再创建会话。
- 刷新或重新登录后恢复用户的工作区和会话列表。
- 会话消息按会话归属加载，RLS 同时限制用户访问范围。

## App Viewer 与发布

- 对话区与应用预览区并列展示。
- 支持 iframe 应用预览和源代码查看。
- 支持生成过程中的计划、代码、应用及完成事件展示。
- 生成应用 HTML 保存到 `projects.app_html`，支持项目预览回放。
- 用户可从 App Viewer 通过 Publish 生成发布链接；修改后的版本需重新发布。

## 资源中心

- “发现”展示 Supabase 中的社区应用并支持分类过滤。
- “模板”展示可复用的应用方案。
- 模板支持查看占位字段说明及可替换内容。
- 用户填写占位内容后可一键生成自定义应用，结果落库至 `projects`。
- 支持体验、克隆、魔改和使用模板等真实后端操作。
- 未登录执行需要账户的操作时显示认证弹窗。
- 连接失败且未配置 Supabase 时回退到演示资源数据。

## 模板占位符生成

- `templates.placeholders` 使用 JSONB 保存模板占位字段、标签、默认值和说明。
- `TemplatePlaceholderDialog` 提供通用占位字段填写弹窗。
- 用户未填写的字段保留模板默认内容。
- 占位值注入演示应用生成器时会分别处理 HTML 和 JavaScript 上下文转义。

## 我的项目

- 展示当前用户真实 Supabase 项目列表。
- 支持全部项目与已收藏项目 Tab 切换。
- 支持收藏和取消收藏并持久化到数据库。
- 支持已保存应用 HTML 的预览回放。
- 查询或更新失败时展示真实错误。
- 未配置 Supabase 时回退到演示项目数据。

## 后端数据与权限

- 提供 `profiles`、`spaces`、`projects`、`conversations`、`messages`、`community_apps` 和 `templates` 业务表。
- `projects` 保存生成应用 HTML，`templates` 通过 `placeholders` 扩展模板配置。
- `conversations.space_id` 将会话绑定到工作区。
- 用户相关表关联 `auth.users` 并启用 RLS。
- profile 和默认工作区初始化采用幂等写入。
- 默认工作区唯一性由迁移脚本约束。
- 社区应用和模板作为公共数据提供访问。

## 博客与内容

- 支持博客索引、文章详情和 Markdown 内容渲染。
- 提供博客预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、开发和部署入口
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # AI 应用生成、超时和重试 Edge Function
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/
│   │   │   ├── 001_initial_schema.sql         # 业务表、RLS 和种子数据
│   │   │   ├── 002_add_app_html.sql           # projects.app_html 字段迁移
│   │   │   ├── 002_template_placeholders.sql  # templates 占位字段迁移
│   │   │   └── 003_space_default_unique.sql   # 默认工作区唯一性约束
│   │   └── scripts/
│   │       ├── deploy_function.py             # 部署 Edge Function
│   │       ├── e2e_verify.py                  # 认证、数据和 RLS 验证
│   │       ├── run_sql.py                     # 执行 Supabase SQL
│   │       ├── set_secrets.py                 # 配置 Edge Function Secrets
│   │       ├── test_gateway.py                # AI 网关流式调用测试
│   │       ├── test_transcribe.py             # 音频转写链路测试
│   │       ├── debug_conversation.py          # 会话创建与消息链路调试
│   │       ├── debug_function_logs.py         # Edge Function 日志调试
│   │       ├── debug_query_auth_users.sql     # auth.users 调试查询
│   │       ├── test_agent_e2e.py              # AI 生成端到端测试
│   │       ├── test_edge_prompt.py            # Edge Function 提示词测试
│   │       └── verify_persisted.py            # 持久化数据核验
│   └── frontend/
│       ├── package.json                        # 前端依赖与脚本
│       ├── vite.config.ts                     # Vite 配置
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── browser-regression.mjs             # 浏览器回归测试
│       ├── tools/
│       │   └── login-session-server.py        # 本地登录会话辅助服务
│       ├── public/                             # favicon、robots 等静态资源
│       ├── prerender/                          # 博客预渲染和站点地图脚本
│       └── src/
│           ├── App.tsx                         # 应用根组件与路由入口
│           ├── main.tsx                        # 前端启动入口
│           ├── pages/                           # 首页、资源、项目和博客页面
│           ├── components/
│           │   ├── auth/                        # 登录和注册弹窗
│           │   ├── layout/                      # 页面布局、工作区和侧边栏
│           │   ├── chat/                        # 对话消息组件
│           │   ├── preview/                     # 应用预览组件
│           │   ├── templates/                   # 模板占位符交互组件
│           │   ├── blog/                        # 博客布局与 Markdown 渲染
│           │   └── ui/                          # 通用 UI 组件
│           ├── contexts/                        # 认证和用户数据上下文
│           ├── hooks/                           # 移动端和 Toast Hooks
│           └── lib/                             # Supabase、智能体、演示数据和工具函数
├── docs/
│   ├── system-design.md                         # 系统设计和技术决策
│   ├── data-model.md                             # 数据模型、工作区、会话与 RLS 说明
│   ├── deployment.md                             # 部署、密钥和验收说明
│   └── usage.md                                  # 使用指南、工作区和会话操作说明
├── assets/
│   └── audios/
│       └── voice-test.mp3                       # 语音转写测试音频
├── uploads/                                     # 需求参考截图
└── .atoms/
    ├── ATOMS.md                                 # 项目约束与上下文说明
    └── PROGRESS.md                              # 任务状态和进度日志
```

# File Description Inventory

| 文件或目录 | 类型 | 用途 |
|---|---|---|
| `README.md` | Markdown | 项目介绍、开发、部署和文档索引 |
| `app/frontend/package.json` | JSON | 前端依赖、检查、构建和预览脚本 |
| `app/frontend/src/pages/HomePage.tsx` | TSX | 首页对话、工作区会话、语音输入和生成流程 |
| `app/frontend/src/pages/ResourcesPage.tsx` | TSX | 资源发现、模板展示和占位符生成入口 |
| `app/frontend/src/pages/ProjectsPage.tsx` | TSX | 项目列表、收藏和预览回放 |
| `app/frontend/src/components/auth/AuthDialog.tsx` | TSX | 登录与注册认证弹窗 |
| `app/frontend/src/components/layout/Sidebar.tsx` | TSX | 工作区选择、会话列表、导航和新建入口 |
| `app/frontend/src/components/chat/` | TSX | 智能体消息和生成状态展示 |
| `app/frontend/src/components/preview/` | TSX | 应用 HTML 预览和源代码查看 |
| `app/frontend/src/components/templates/TemplatePlaceholderDialog.tsx` | TSX | 模板占位字段说明、填写和生成弹窗 |
| `app/frontend/src/lib/agent.ts` | TypeScript | Edge Function 流式调用、超时检测和演示回退 |
| `app/frontend/src/lib/supabase.ts` | TypeScript | Supabase 客户端、认证、工作区、会话和数据访问 |
| `app/frontend/src/contexts/AuthContext.tsx` | TSX | 用户认证状态、会话恢复和 profile/工作区初始化 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | TypeScript | 调用 AI 网关并输出 SSE 生成事件 |
| `app/backend/functions/app_atoms_transcribe_audio/index.ts` | TypeScript | 通过平台 AI 网关执行音频转写 |
| `app/backend/migrations/001_initial_schema.sql` | SQL | 初始化业务表、RLS 和种子数据 |
| `app/backend/migrations/002_add_app_html.sql` | SQL | 为项目增加生成应用 HTML 存储字段 |
| `app/backend/migrations/002_template_placeholders.sql` | SQL | 为模板增加占位字段和种子数据 |
| `app/backend/migrations/003_space_default_unique.sql` | SQL | 防止同一用户重复创建默认工作区 |
| `app/backend/scripts/` | Python/SQL | 数据库执行、函数部署、调试和端到端验证工具 |
| `app/frontend/browser-regression.mjs` | JavaScript | 浏览器级认证、会话、生成和项目回归测试 |
| `app/frontend/tools/login-session-server.py` | Python | 为浏览器回归提供登录会话辅助能力 |
| `assets/audios/voice-test.mp3` | MP3 | 服务端转写端到端测试素材 |
| `app/frontend/prerender/` | JavaScript | 博客页面预渲染和站点地图生成 |
| `docs/*.md` | Markdown | 系统设计、数据模型、部署和使用文档 |
| `.atoms/ATOMS.md` | Markdown | 当前项目约束和开发上下文 |
| `.atoms/PROGRESS.md` | Markdown | 任务进度与完成记录 |
| `uploads/*.png` | PNG | 产品需求及界面参考资料 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18 + Vite | 单页应用开发与构建 |
| 语言 | TypeScript | 前端类型安全开发 |
| 样式 | Tailwind CSS | 原子化样式系统 |
| UI 组件 | shadcn/ui、Radix UI | 可复用交互组件 |
| 路由 | React Router | 页面导航 |
| 状态与数据 | React Context、Supabase、演示回退 | 认证、工作区、会话、项目和资源管理 |
| 后端平台 | Supabase | Auth、PostgreSQL、RLS 和 Edge Functions |
| 数据库 | PostgreSQL | 用户、工作区、会话、消息、项目和公共资源存储 |
| 智能体 | `deepseek-v4-flash`、AI 网关、SSE | 计划生成、代码生成和实时输出 |
| 语音转写 | `scribe_v2`、AI 网关、Edge Function | 音频转文字及服务端密钥隔离 |
| Edge Function | Deno/TypeScript | 服务端 AI 调用、超时控制、重试和音频转写 |
| 外部连接 | MCP | 远程工具与工作区连接 |
| 内容处理 | Markdown、博客预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright、Python E2E 脚本 | 浏览器回归、认证、数据和生成链路验证 |
| 代码规范 | ESLint、TypeScript strict | 静态检查与类型校验 |
| 交付 | GitHub main 分支 | 版本管理和代码交付 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。Supabase 已配置时，真实请求失败会显示错误；未配置环境变量时才使用演示数据和本地演示智能体。AI 生成支持软超时、流中断检测和不完整 HTML 重试。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用 npm：

```bash
npm install
```

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置。不要将 `.env.local` 提交到版本库。

服务端配置 AI 网关地址和密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

具体变量和部署要求参见 `README.md`、`docs/deployment.md` 和 `app/backend/README.md`。

## 初始化数据库

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
```

初始化内容包括业务表、RLS 策略、公共资源种子数据、项目 HTML 字段、模板占位数据及默认工作区唯一性约束。

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

部署后确认应用生成函数和音频转写函数均已发布，并配置服务端 Secrets。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

未配置 Supabase 时，前端使用演示资源和本地演示智能体。已配置 Supabase 时，真实链路错误会直接反馈。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
```

## 生产预览

```bash
cd app/frontend
pnpm preview
```

## 后端与端到端验证

```bash
cd app/backend
python scripts/e2e_verify.py
python scripts/test_gateway.py
python scripts/test_transcribe.py
python scripts/test_agent_e2e.py
python scripts/verify_persisted.py
```

浏览器回归测试：

```bash
cd app/frontend
node browser-regression.mjs
```

验证内容包括注册登录、profile 与默认工作区初始化、工作区创建与切换、会话和消息隔离、RLS、AI 流式响应、生成超时与重试、项目落库、收藏、模板生成和语音转写。

## 使用流程

1. 注册或登录账户。
2. 系统自动创建默认工作区；也可在工作区选择器中创建新工作区。
3. 选择工作区并点击新建会话，或进入当前工作区的已有会话。
4. 输入应用需求并提交，或点击麦克风录音。
5. 查看生成计划、流式代码和实时预览。
6. 在资源页打开模板，查看占位字段说明并填写自定义内容。
7. 提交模板生成，应用会保存至我的项目并支持预览回放。
8. 切换工作区，确认最近会话仅显示当前工作区内容。
9. 在我的项目中收藏或回放应用。
10. 在 App Viewer 中通过 Publish 发布最新版本。
