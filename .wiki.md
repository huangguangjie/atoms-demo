# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，由 AI Agent 规划并生成可运行、可预览、可编辑、可保存和可发布的单文件 HTML/JavaScript 应用，支持认证、工作区、会话恢复、项目管理、增量修改、版本管理、演示模式和会话归档。

当前流程为“首页输入 → 会话详情开发 → 应用预览与发布”：

- 首页创建会话，历史会话可恢复消息、项目和版本。
- 详情页提供 Chat、计划、Agent 输出、应用预览、源码、在线编辑、版本管理和 Publish。
- 已有项目通过 `previousHtml` 进行增量修改。
- 生成产物经清理、产物守卫和内联 JavaScript 语法校验后持久化。
- 项目、会话、消息和版本由 Supabase 持久化，并通过 RLS 隔离。
- 版本写入统一使用原子 RPC，Preview 使用安全沙箱。
- 失败产物不会进入 Preview、源码或数据库；版本写入失败时项目、快照与版本号整体回滚。
- 失败状态会保留历史消息与成功版本，并支持幂等重试和刷新恢复。
- 构建产物注入 Git SHA、分支和构建时间，用于线上部署溯源。
- T32 已完成失败回退、版本一致性、Preview/源码一致性、刷新恢复和重试幂等加固。
- T32 失败回退专项走查 35/35 通过；T31 事务注入 20/20、真实模型矩阵 44/44、认证恢复 21/21、版本一致性 26/26 通过。
- reviewer 账号已彻底回收，相关脚本可重复执行且幂等。

# Project Module Description

## 工作区、认证与会话

- 支持邮箱注册、登录、退出、重新登录和工作区切换。
- 注册触发器幂等创建 profile 与默认工作区。
- Supabase Auth、PostgreSQL 和 RLS 隔离用户、工作区、会话、消息、项目及版本。
- 支持刷新、清空存储和全新浏览器会话后的登录状态恢复。
- 侧边栏支持折叠、响应式布局、收藏、重命名和删除会话。
- 历史会话可进入详情页恢复消息、项目和版本。

## 会话归档

- `conversations.status` 使用 `active` 和 `archived`。
- 活跃历史查询统一过滤 `status='active'`。
- 归档会话旧链接显示归档提示并返回首页。
- 归档不删除会话、消息、项目或版本。
- 指定账户保留最近 5 条会话及全部收藏会话，其余标记为 `archived`。
- 恢复归档会话需在数据库层改回 `active`。

## Chat 输入区

- 首页与详情页共用 `ChatComposer`。
- 支持文本、附件、语音转写、MCP、主题、构建模式和目标模式。
- `+` 菜单提供附件、MCP、视频、深度研究和竞赛模式入口。
- `#` 可引用上传内容、AI、密钥和项目。
- 支持过滤、键盘导航、Escape、外部点击关闭及中文输入法组合输入。
- 用户消息先更新界面，再写入数据库。

## 会话详情与应用查看

- 详情页采用顶部工具栏、左侧 Chat、右侧 App Viewer 布局。
- 支持 SSE 流式生成、停止生成、错误恢复、重新生成和历史消息加载。
- App Viewer 支持 iframe 预览、源码查看、在线编辑、版本面板和 Publish。
- Preview、源码查看器和数据库 `app_html` 始终指向当前生效产物。
- 只有版本写入成功后，Preview、源码和当前项目状态才会切换。
- 修改应用后需重新 Publish 才能更新已发布站点。
- 刷新、退出登录或新建会话后，可恢复项目、对话、源码、版本列表、失败状态和 Preview。

## Agent 生成管线

生成链路为：

1. 意图识别。
2. 布局规划。
3. 代码生成，并施加单文件 HTML、内联 CSS/JavaScript 和功能保留等硬约束。
4. `sanitizeHtml` 清理产物。
5. 产物守卫检查空内容、截断内容和内联 JavaScript 语法。
6. 必要时最多进行一次内容保全式修复。
7. 通过 SSE 发送计划、步骤、代码增量、应用产物和终止状态。
8. 前端在版本写入成功后更新项目、源码和 Preview。

SSE 事件包括 `plan`、`step`、`code-delta`、`app`、`done` 和 `error`。`done` 是正常终止信号；`error` 不会自动继续，用户需主动重新生成。

- `deliveredApp` 防止连接截断后重复规划或生成。
- `appPersisted` 防止重复 `app` 事件重复写入。
- `flowLockRef` 防止重复点击重新生成造成重复消息和版本。
- 未交付应用的瞬时错误最多自动重试 4 次。
- 真实 AI 通道失败会明确显示错误，不静默替换为演示应用。
- 演示模式必须由用户主动触发，并保留演示标识。

## 增量修改与版本管理

- 首次生成使用普通生成流程。
- 已有项目修改时传递 `previousHtml`，要求保留未修改功能和内容。
- 每轮成功生成、在线编辑和回滚都会创建版本快照。
- `project_versions` 保存版本号、来源、创建时间和 `app_html`。
- 版本来源包括 `generation`、`edit` 和 `rollback`。
- 回滚创建新的 `rollback` 版本，不删除旧版本。
- 支持历史版本预览、切换、回滚和 HTML 导出。
- 版本切换仅更新本地预览，不会写入数据库。
- 刷新后根据消息 metadata 恢复生成状态、失败状态和应用产物。

### 原子版本写入

- 版本写入统一调用 RPC `app_write_project_version`，由迁移 009 提供。
- RPC 在单个事务内完成“取号 → 更新项目 `app_html` → 插入版本快照”。
- `UNIQUE(project_id, version_number)` 处理并发冲突，冲突方以最新版本号重试。
- 函数为 `SECURITY INVOKER`，设置 `search_path = public`，并在函数内校验 `auth.uid()`。
- 未登录抛 `28000`，项目不存在抛 `P0002`，非属主抛 `42501`，非法 `source` 抛 `22023`。
- 参数签名包含 `p_version_number`（为空时由函数权威取号）与 `p_is_demo`。
- 任一语句失败或连接中断时整体回滚，避免项目与快照出现半写状态。
- 首次生成、增量修改、在线编辑和回滚统一使用该路径。
- 失败不会占用版本号，不会留下空快照或孤儿快照。
- 线上核验确认 RPC 唯一无重载、`authenticated` 具备 EXECUTE、半写项目 0、版本跳号 0、孤儿快照 0、空快照 0。

## 失败回退与幂等加固

- 产物在落库与展示之前经 `artifact-guard` 校验：空内容、未以 `</html>` 收尾的截断产物、内联脚本语法错误一律判为无效。
- 无效产物不进入 Preview、不写入源码查看器、不写入数据库。
- 只有 RPC 成功返回后才切换 Preview、源码与当前项目状态。
- 失败时 `projects.app_html` 保持失败前的成功版本，Preview 与源码继续显示上一成功版本。
- 失败不产生新版本号、不占用号段、不留下不一致快照。
- 失败助手消息落库并标记 `failed`，metadata 记录错误原因和原始 prompt。
- 刷新后可恢复失败卡、原始需求、计划、消息、版本列表和重试入口。
- 重试只追加新的助手消息，不重复落库用户消息、不重复创建项目。
- 流程锁在所有异常路径释放，重复点击不会产生重复写入。
- 成功重试只创建一个新版本和一条助手消息。
- 公开表读取遇到瞬时 401 时自动刷新会话并有限重试，避免恢复流程得到错误空列表。
- T32 失败回退与幂等专项回归 35/35 通过。
- reviewer 账号 `t31.reviewer@atoms-demo.dev` 已彻底回收，级联资料、工作区、项目、版本与 `auth.users` 均已清理。

## 产物运行沙箱与部署溯源

- Preview 统一经 `lib/preview-sandbox.ts` 包装。
- iframe 使用 `sandbox` 白名单与 `srcdoc`，运行在 opaque origin。
- CSP 限制脚本与网络外发，表单提交默认阻止，`window.open` 与顶层导航被拦截。
- AI 产物无法访问宿主 DOM、`localStorage`、`sessionStorage`、Cookie 或登录令牌。
- 构建期注入 Git SHA、ref 和构建时间到 `window.__ATOMS_BUILD__`。
- 线上可通过 `window.__ATOMS_BUILD__` 核对部署产物对应的构建提交。

## 产物校验

- Edge Function 只对内联 JavaScript 做语法检查，不执行产物代码。
- 前后端共享产物有效性语义。
- 语法错误时最多进行一次保持内容和功能不变的修复。
- 修复后重新清理、校验并交付，避免 Preview iframe 因 AI 输出错误而失败。

## 主题、界面与构建模式

- 主题按钮使用 `Palette` 图标，默认不选择主题。
- 未选择主题时不注入主题提示词，并使用中性色板作为预览回退。
- 明确选择主题后才影响生成和预览，刷新后保留选择。
- 构建类型和目标模式显示名称，菜单选中项的对勾位于右侧。
- 详情页对话栏、状态卡、源码查看器和 HTML 编辑器统一使用浅色体系。
- 用户消息气泡和主题预览卡保留必要的深色或半透明对比。

## 资源、模板、项目与博客

- 音频通过 `app_atoms_transcribe_audio` Edge Function 转换为文本。
- 模板支持占位字段填写、体验、克隆和魔改，并对 HTML/JavaScript 上下文进行转义。
- 我的项目展示真实项目，支持收藏、预览、版本查看和历史回放。
- 博客支持索引、文章详情、Markdown 渲染、预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、运行、部署和验证说明
├── PROJECT_NOTES.md                           # 架构设计、完成度和规划
├── .wiki.md                                   # 项目 Wiki 汇总说明
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # AI 生成、增量修改、校验和 SSE
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/                        # 数据库结构、版本、归档和关联迁移
│   │   ├── reports/                           # 验收证据和账户回收脚本
│   │   └── scripts/                           # 数据整理、部署和诊断脚本
│   └── frontend/
│       ├── package.json                       # 前端依赖和脚本
│       ├── vite.config.ts                     # Vite 配置及构建溯源注入
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── public/                            # 静态资源
│       ├── prerender/                         # 博客预渲染和站点地图脚本
│       ├── tools/                             # 本地登录会话辅助工具
│       ├── t31-*.mjs                          # T31 认证、模型、沙箱和事务回归
│       ├── t32-failure-rollback-walkthrough.mjs # T32 失败回退专项走查
│       ├── *-result.json                      # 回归结果证据
│       └── src/
│           ├── App.tsx                        # 根组件与路由布局
│           ├── main.tsx                       # 前端启动入口
│           ├── pages/
│           │   ├── HomePage.tsx               # 首页输入和会话创建
│           │   └── ChatDetailPage.tsx         # 详情页、生成、版本和归档
│           ├── components/                    # 布局、聊天、预览、版本和 UI
│           ├── contexts/                      # 认证和用户数据上下文
│           ├── hooks/                         # 通用 Hooks
│           └── lib/
│               ├── agent.ts                   # Edge Function 调用、SSE 和重试
│               ├── artifact-guard.ts          # 产物完整性和语法守卫
│               ├── preview-sandbox.ts         # Preview iframe 沙箱包装
│               ├── supabase.ts                # 数据访问和原子版本写入
│               └── demo-apps.ts               # 演示应用和主题资源
├── docs/
│   ├── system-design.md                       # 产品设计、分层架构和生成时序
│   ├── data-model.md                          # 数据表、字段、迁移和 RLS
│   ├── deployment.md                          # 部署、模型配置、溯源和回归记录
│   └── usage.md                               # 使用流程和界面规范
├── assets/                                    # 测试音频等资源
└── .atoms/
    ├── ATOMS.md                               # 项目约束、决策与上下文
    ├── ARCHITECTURE.md                        # 当前架构状态和关键技术决策
    └── PROGRESS.md                            # T1-T32 工作状态与进度日志
```

临时截图、诊断输出、构建产物、`.tmp-*`、测试结果缓存和本地环境文件不属于产品运行资源，也不应提交到版本控制。

# File Description Inventory

| 文件或目录 | 用途 |
|---|---|
| `README.md` | 功能、运行、部署、回归和当前状态说明 |
| `PROJECT_NOTES.md` | 产品取舍、架构、任务完成度和后续规划 |
| `.wiki.md` | Wiki 汇总与文档交叉索引 |
| `.atoms/ARCHITECTURE.md` | 分层架构、Agent 管线、原子写入和失败语义 |
| `.atoms/PROGRESS.md` | T1–T32 任务状态、证据和发布记录 |
| `app/frontend/src/App.tsx` | 根路由及页面布局切换 |
| `app/frontend/src/pages/HomePage.tsx` | 首页输入、主题状态、项目查询和会话创建 |
| `app/frontend/src/pages/ChatDetailPage.tsx` | 详情布局、生成、增量修改、失败回退、重试和版本写入 |
| `app/frontend/src/components/chat/ChatComposer.tsx` | 输入区、菜单、引用、主题和发送控制 |
| `app/frontend/src/components/chat/ChatMessage.tsx` | 消息、计划卡、失败状态和演示标识展示 |
| `app/frontend/src/components/layout/Sidebar.tsx` | 工作区、活跃历史会话和会话操作 |
| `app/frontend/src/components/preview/AppPreview.tsx` | App Viewer、源码查看和在线编辑 |
| `app/frontend/src/lib/agent.ts` | SSE 调用、事件解析、产物守卫、终止和重试 |
| `app/frontend/src/lib/artifact-guard.ts` | 空内容、截断内容和 JavaScript 语法校验 |
| `app/frontend/src/lib/preview-sandbox.ts` | Preview 沙箱、CSP 和宿主访问隔离 |
| `app/frontend/src/lib/supabase.ts` | 会话、项目、消息、版本和原子写入查询 |
| `app/frontend/src/lib/demo-apps.ts` | 演示应用、主题色板和演示资源 |
| `app/frontend/vite.config.ts` | 构建配置及 `__ATOMS_BUILD__` 注入 |
| `app/frontend/t32-failure-rollback-walkthrough.mjs` | 失败回退、快照一致性、幂等、恢复和 401 自愈回归 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | AI 管线、模型调用、增量上下文、语法校验和 SSE |
| `app/backend/functions/app_atoms_transcribe_audio/index.ts` | 音频转文字 |
| `app/backend/migrations/009_atomic_version_write.sql` | 原子版本写入 RPC、权限和事务约束 |
| `app/backend/reports/t32-reviewer-revoke-exec.sql` | reviewer 账户彻底回收与幂等核验 |
| `app/backend/scripts/t32_columns_probe.sql` | 数据库字段结构探测 |
| `app/backend/scripts/t32_inconsistency_probe.sql` | 项目与最新快照一致性核验 |
| `app/backend/scripts/t32_rpc_probe.sql` | RPC 签名、权限、安全属性和数据一致性核验 |
| `docs/system-design.md` | 产品设计、架构分层、生成管线、事务和失败语义 |
| `docs/data-model.md` | 核心表、迁移顺序、原子 RPC、索引和 RLS 策略 |
| `docs/deployment.md` | Supabase、Edge Function、模型配置、构建溯源和回归 |
| `docs/usage.md` | 产品操作流程、Publish 说明和界面规范 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18、Vite | 单页应用和页面构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS、CSS | 页面、响应式样式和浅色主题 |
| UI 组件 | shadcn/ui、Radix UI | 菜单、Popover、选择器和交互组件 |
| 图标 | lucide-react | 导航、主题和操作图标 |
| 路由 | React Router | 页面导航 |
| 状态与数据 | React Context、Supabase | 认证、会话、消息、项目和版本 |
| 后端平台 | Supabase Auth、PostgreSQL、RLS、Edge Functions | 认证、持久化、数据隔离和服务端逻辑 |
| 智能体 | DeepSeek、GPT、Gemini、AI 网关、SSE | 规划、代码生成、增量修改和流式传输 |
| Edge Function | Deno、TypeScript | AI 调用、重试、清理和语法校验 |
| 安全运行 | iframe sandbox、`srcdoc`、CSP | AI 产物隔离和宿主保护 |
| 构建溯源 | Vite define、Git SHA | 线上版本与仓库提交对应 |
| 测试打包 | esbuild、Node.js | Agent mock 测试和脚本执行 |
| 语音转写 | `scribe_v2`、AI 网关 | 音频转文字 |
| 外部连接 | MCP | 远程工具和工作区连接 |
| 内容处理 | Markdown、预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright、Node、Python | 浏览器回归、诊断和 E2E |
| 代码规范 | ESLint、TypeScript strict | 静态检查和类型校验 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。演示模式使用本地演示资源。reviewer 凭据不进入仓库，仅通过独立交付文件提供，并配套到期回收脚本。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用：

```bash
npm install
```

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置，不要提交 `.env.local`。

在后端配置 AI 网关和服务端密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

## 初始化数据库

按迁移顺序执行：

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
python scripts/run_sql.py migrations/004_user_init_trigger.sql
python scripts/run_sql.py migrations/005_conversation_actions.sql
python scripts/run_sql.py migrations/006_project_versions_and_metadata.sql
python scripts/run_sql.py migrations/007_conversation_archive_status.sql
python scripts/run_sql.py migrations/008_project_conversation_link.sql
python scripts/run_sql.py migrations/009_atomic_version_write.sql
```

迁移 009 发布后，确认 RPC `app_write_project_version` 仅允许项目所有者调用，并核验匿名执行权限已收紧。

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

确认应用生成和音频转写函数已发布，并配置服务端 Secrets。生成函数应包含增量保留约束、产物清理和 JavaScript 语法校验。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

真实 AI 链路可直接使用；未配置真实 AI 时，只能由用户主动触发演示模式。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
pnpm preview
```

质量门禁：

- `pnpm lint` 无错误。
- Vite 构建成功，构建包含 SHA、ref 和时间信息。
- 博客首页和博客路由支持预渲染。
- T31 与 T32 回归脚本通过。
- RPC 与数据一致性核验中半写、跳号、孤儿快照和空快照均为 0。
- 敏感文件、依赖目录、构建目录和临时产物不进入版本控制。

## 回归验证

```bash
cd app/frontend
node t31-auth-walkthrough.mjs
node t31-sandbox-security.mjs
node t31-real-model-matrix.mjs
node t31-transaction-injection.mjs
node t32-failure-rollback-walkthrough.mjs
node t28-loop-repro.mjs
node t29-real-chain-walkthrough.mjs
node t27-light-theme-walkthrough.mjs
node t26-archive-walkthrough.mjs
node t25-versioning-walkthrough.mjs
node t23-demo-mode-walkthrough.mjs
node t22-theme-icon-walkthrough.mjs
node t13-chat-detail-regression.mjs
node t10-sidebar-actions-regression.mjs
node t9-agent-quality-regression.mjs
node t8-input-regression.mjs
node t7-workspace-regression.mjs
node browser-regression.mjs
```

已确认结果：

- T32 失败回退与幂等：35/35 通过。
- T31 认证与状态恢复：21/21 通过。
- T31 Preview 沙箱安全：24/24 通过。
- T31 真实模型连续生成矩阵：44/44 通过。
- T31 事务与中断注入：20/20 通过。
- T31 版本一致性：26/26 通过。
- T31 reviewer 账号与 RLS 隔离：10/10 通过。
- T28 Mock SSE：11/11 通过。
- T29 真实 AI 全链路：32/32 通过。
- T26 归档回归：14/14 通过。
- T27 浅色体系回归：17/17 通过。
- lint、Vite 构建和预渲染均通过。
- 构建溯源注入值已验证与当次构建提交一致。

## 使用流程

1. 注册或登录账户。
2. 在首页输入应用需求，可添加附件、语音或 MCP。
3. 可选选择构建类型和主题后提交。
4. 在详情页查看计划、流式输出和消息历史。
5. 在右侧预览应用或查看源码。
6. 基于当前项目提交增量修改。
7. 在版本面板切换历史版本或执行回滚。
8. 使用在线编辑器修改 HTML，保存后同步 Preview。
9. 导出当前版本 HTML。
10. 刷新详情页，确认消息、项目、版本和生成状态恢复。
11. 生成失败时使用失败卡或输入区重试；失败不会污染 Preview、源码与已有版本。
12. 在 App Viewer 右上角点击 Publish 更新已发布站点。
13. 发布后检查 `window.__ATOMS_BUILD__`，核对线上版本对应的提交 SHA。

本轮前端和生成链路变更不会自动更新已发布站点；完成升级后需重新点击 Publish。
