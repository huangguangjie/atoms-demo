# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，由 AI 智能体生成可预览、可编辑、可保存和可发布的 HTML/JavaScript 应用，并支持会话恢复、项目管理、演示模式、增量修改、版本管理和会话归档。

当前产品流程为“首页输入 → 会话详情开发 → 应用预览与发布”：

- 首页创建会话，历史会话可恢复消息和关联项目。
- 详情页提供对话、执行计划、Agent 输出、应用预览、源码、版本管理和 Publish。
- 已有项目可基于 `previousHtml` 进行连续增量修改。
- 项目支持版本切换、预览、回滚、在线 HTML 编辑和导出。
- 非活跃会话可按规则归档，不删除会话、消息、项目或版本数据。
- 详情页及源码、编辑器、菜单统一采用浅色界面体系。
- 已修复生成完成后因 SSE 缺少 `done` 或连接异常而重新规划、重新生成的死循环。
- 真实 AI 链路已恢复可用；演示模式仍可由用户显式选择。

# Project Module Description

## 工作区、认证与会话

- 支持邮箱注册、登录、退出和工作区切换。
- 注册时通过数据库触发器幂等创建 profile 与默认工作区。
- Supabase Auth、PostgreSQL 和 RLS 隔离用户、工作区、会话、消息及项目数据。
- 侧边栏支持折叠、窄屏布局、收藏、重命名和删除会话。
- 历史会话可进入详情页恢复消息、项目和版本。

## 会话归档

- `conversations.status` 使用 `active` 和 `archived` 状态。
- 活跃历史查询统一过滤 `status='active'`。
- 归档会话旧链接直达时显示归档提示并返回首页。
- 归档不删除会话、消息、项目或版本数据。
- 指定账户按规则保留最近 5 条会话及全部收藏会话，其余标记为 `archived`。
- 恢复归档会话需在数据库层将状态改回 `active`。

## Chat 输入区

- 首页与详情页共用 `ChatComposer`。
- 支持文本、附件、语音转写、MCP、主题、构建模式和目标模式。
- `+` 菜单提供附件、MCP、视频、深度研究和竞赛模式入口。
- 输入 `#` 可引用上传内容、AI、密钥和项目。
- 支持过滤、键盘导航、Escape 关闭、点击外部区域关闭及中文输入法组合输入。
- 用户消息先更新界面，再写入数据库。

## 会话详情与应用查看

- 详情页采用顶部工具栏、左侧 Chat、右侧 App Viewer 的布局。
- 支持 SSE 流式生成、停止生成、错误恢复、重新生成和历史消息加载。
- App Viewer 支持 iframe 预览、源码查看、在线编辑、版本面板和 Publish。
- Preview 与源码页签同步当前产物。
- 修改应用后需要重新 Publish。

## Agent 生成与终止保护

生成流程包括意图识别、布局规划、代码生成、产物清理、持久化和版本写入，并通过 `plan`、`message`、`step`、`app`、`done`、`error` SSE 事件传递状态。

T28 修复内容：

- 发现前端在已收到 `app` 产物后，若 SSE 流未收到 `done` 就把连接结束误判为瞬时错误。
- 该判断会触发完整 `runAgent` 重试，导致“规划 → 代码 → 100% → 重新规划”的循环。
- `deliveredApp` 守卫确保产物交付后，即使流截断或收尾异常，也不会重新规划或重新生成。
- 自动重试仅适用于尚未产生应用产物的瞬时错误，完整上游调用最多 4 次。
- `app` 事件使用 `appPersisted` 幂等标记，避免重复写入项目、消息和版本快照。
- `done` 是唯一正常终止信号；`error` 不会自动继续，只有用户点击“重新生成”才会重新执行。

## 增量修改与版本管理

- 首次生成使用普通生成流程。
- 已有项目继续修改时传递 `previousHtml`。
- Edge Function 将上一轮 HTML 注入增量修改上下文，并强化保留既有功能和文案的约束。
- 每轮结果更新项目当前 HTML、Preview、源码和版本历史。
- `project_versions` 保存版本号、来源、创建时间和 `app_html` 快照。
- 版本来源包括 `generation`、`edit` 和 `rollback`。
- 回滚创建新的 `rollback` 版本，不删除旧版本。
- 支持版本历史、切换查看、在线编辑保存和 HTML 导出。
- 同一生成轮次的重复 `app` 事件不会重复写入版本。

## 产物质量校验

- Edge Function 在交付前对内联 JavaScript 执行纯语法校验，不执行产物代码。
- 检测到语法错误时，最多执行一次保持内容和功能不变的修复重试。
- 修复后重新清理并交付产物。
- 该机制用于避免 AI 输出 `const` 声明缺少初始化器等错误导致 Preview iframe 报错。

## 主题与构建模式

- 主题按钮使用 `Palette` 图标，默认不选择主题。
- 未选择主题时不注入主题提示词，并使用中性色板作为预览回退。
- 明确选择主题后才影响生成和预览，刷新后保留选择。
- 构建类型和目标模式展示名称，菜单选中项的对勾位于右侧。
- 详情页对话栏、状态卡、源码查看器和 HTML 编辑器统一使用白色浅色体系。
- 用户消息气泡和主题预览卡装饰条保留有意的深色或半透明对比。

## 失败处理与演示模式

- 生成失败卡提供重新生成入口。
- 确定性错误直接显示失败提示，不触发自动重试。
- 瞬时错误仅在未交付应用时自动重试。
- 用户可主动选择演示模式生成本地演示应用。
- 演示应用正常落库，并在消息 metadata、项目字段和 App Viewer 中显示演示标识。
- 演示模式不会静默替代真实 AI。

## 语音、模板、项目与博客

- 音频通过 `app_atoms_transcribe_audio` Edge Function 转换为文本。
- 模板支持占位字段填写、体验、克隆和魔改，输入按 HTML/JavaScript 上下文转义。
- 我的项目展示真实项目，支持收藏、预览、版本查看和历史回放。
- 博客支持索引、文章详情、Markdown 渲染、预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、运行、部署和验证说明
├── PROJECT_NOTES.md                           # 架构设计、完成度和规划
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # AI 生成、增量修改、语法校验和 SSE
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/                        # 数据库结构、版本、归档和 RLS
│   │   └── scripts/                           # 数据整理和诊断脚本
│   └── frontend/
│       ├── package.json                       # 前端依赖和脚本
│       ├── vite.config.ts                     # Vite 配置
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── public/                            # 静态资源
│       ├── prerender/                         # 博客预渲染和站点地图脚本
│       ├── tools/                             # 本地登录会话辅助工具
│       ├── t28-agent-entry.ts                 # T28 Agent 测试打包入口
│       ├── t28-loop-repro.mjs                 # SSE 死循环复现与回归脚本
│       ├── t28-stub-supabase.ts               # T28 Supabase 测试桩
│       ├── t29-real-chain-walkthrough.mjs     # T29 真实 AI 全链路走查
│       └── src/
│           ├── App.tsx                        # 根组件与路由布局
│           ├── main.tsx                       # 前端启动入口
│           ├── pages/
│           │   ├── HomePage.tsx               # 首页输入和会话创建
│           │   └── ChatDetailPage.tsx         # 详情页、生成、版本和归档守卫
│           ├── components/                    # 布局、聊天、预览、版本和 UI
│           ├── contexts/                      # 认证和用户数据上下文
│           ├── hooks/                         # 通用 Hooks
│           └── lib/
│               ├── agent.ts                   # Edge Function 调用、SSE 解析和重试
│               ├── supabase.ts                # 会话、项目、消息和版本查询
│               └── demo-apps.ts               # 演示应用和主题资源
├── docs/
│   ├── system-design.md                       # 系统设计、架构图、时序图、SSE 与终止守卫
│   ├── data-model.md                          # 数据表、字段和 RLS 说明
│   ├── deployment.md                           # 部署、模型配置和回归记录
│   └── usage.md                               # 使用流程和界面规范
├── assets/                                    # 测试音频等资源
└── .atoms/
    ├── ATOMS.md                               # 项目约束、决策与上下文
    └── PROGRESS.md                            # T1-T29 工作状态与进度日志
```

临时截图、诊断脚本输出、构建产物和 `.tmp-*` 文件仅用于验证，不属于产品运行所需资源。

# File Description Inventory

| 文件或目录 | 用途 |
|---|---|
| `README.md` | 项目功能、运行、部署、回归和已知问题说明 |
| `PROJECT_NOTES.md` | 产品取舍、技术架构、任务完成度和后续规划 |
| `app/frontend/src/App.tsx` | 根路由及页面布局切换 |
| `app/frontend/src/pages/HomePage.tsx` | 首页输入、主题状态、项目查询和会话创建 |
| `app/frontend/src/pages/ChatDetailPage.tsx` | 详情布局、生成、增量修改、版本管理和归档处理 |
| `app/frontend/src/components/chat/ChatComposer.tsx` | 输入区、菜单、引用、主题和发送控制 |
| `app/frontend/src/components/chat/ChatMessage.tsx` | 消息、计划卡和演示标识展示 |
| `app/frontend/src/components/layout/Sidebar.tsx` | 工作区、活跃历史会话和会话操作 |
| `app/frontend/src/components/preview/AppPreview.tsx` | App Viewer 预览、源码查看和编辑器 |
| `app/frontend/src/lib/agent.ts` | SSE 调用、事件解析、终止守卫、重试和错误透传 |
| `app/frontend/src/lib/supabase.ts` | 会话、项目、消息、版本和归档查询 |
| `app/frontend/src/lib/demo-apps.ts` | 演示应用、主题色板和演示资源 |
| `app/frontend/t28-agent-entry.ts` | 为 T28 mock SSE 测试打包 Agent 入口 |
| `app/frontend/t28-loop-repro.mjs` | 复现修复前后生成循环并输出 JSON 结果 |
| `app/frontend/t28-stub-supabase.ts` | T28 测试环境的 Supabase stub |
| `app/frontend/t29-real-chain-walkthrough.mjs` | 真实生成、增量修改、版本操作和刷新恢复验证 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | AI 管线、模型降级、增量上下文、语法校验和 SSE |
| `app/backend/functions/app_atoms_transcribe_audio/index.ts` | 音频转文字 |
| `app/backend/migrations/006_project_versions_and_metadata.sql` | 项目版本和演示元数据结构 |
| `app/backend/migrations/007_conversation_archive_status.sql` | 会话归档状态字段和约束 |
| `app/backend/scripts/t26_archive_conversations.sql` | 按保留规则批量归档指定账户会话 |
| `docs/data-model.md` | 数据表、字段、关联、状态和 RLS 说明 |
| `docs/deployment.md` | 部署、模型配置、错误分类、产物校验和回归说明 |
| `docs/usage.md` | 产品使用流程及界面浅色配色规范 |
| `.atoms/PROGRESS.md` | T1-T29 任务状态、证据和发布记录 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18、Vite | 单页应用和页面构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS、CSS | 页面、响应式样式和布局 |
| UI 组件 | shadcn/ui、Radix UI | 菜单、Popover、选择器和交互组件 |
| 图标 | lucide-react | 导航、主题和操作图标 |
| 路由 | React Router | 首页与会话详情导航 |
| 状态与数据 | React Context、Supabase | 认证、会话、消息、项目和版本 |
| 后端平台 | Supabase Auth、PostgreSQL、RLS、Edge Functions | 认证、数据隔离和服务端逻辑 |
| 智能体 | DeepSeek、GPT、Gemini、AI 网关、SSE | 规划、代码生成、增量修改和流式传输 |
| Edge Function | Deno、TypeScript | AI 调用、重试、产物清理和语法校验 |
| 测试打包 | esbuild、Node.js | T28 Agent mock 测试和脚本执行 |
| 语音转写 | `scribe_v2`、AI 网关 | 音频转文字 |
| 外部连接 | MCP | 远程工具和工作区连接 |
| 内容处理 | Markdown、预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright、Node、Python | 浏览器回归、诊断和 E2E |
| 代码规范 | ESLint、TypeScript strict | 静态检查和类型校验 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。演示模式使用本地演示资源。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用：

```bash
npm install
```

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置，不要提交 `.env.local`。

在后端配置 AI 网关和服务端密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

## 初始化数据库

按迁移顺序执行数据库迁移：

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
python scripts/run_sql.py migrations/004_user_init_trigger.sql
python scripts/run_sql.py migrations/005_conversation_actions.sql
python scripts/run_sql.py migrations/006_project_versions_and_metadata.sql
python scripts/run_sql.py migrations/007_conversation_archive_status.sql
python scripts/run_sql.py migrations/008_project_conversation_link.sql
```

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

确认应用生成和音频转写函数已发布，并配置服务端 Secrets。T29 后 Edge Function 还需包含增量保留约束和产物 JavaScript 语法校验。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

真实 AI 链路可直接使用；未配置真实 AI 时，可使用用户主动触发的演示模式。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
pnpm preview
```

当前质量门禁：

- `pnpm lint`：0 个错误。
- Vite build：成功。
- 博客首页和博客路由支持预渲染。
- T23、T25、T26、T27、T28、T29 及既有回归脚本已覆盖核心流程。

## 回归验证

```bash
cd app/frontend
node t28-loop-repro.mjs
node t29-real-chain-walkthrough.mjs
node t27-light-theme-walkthrough.mjs
node t26-archive-walkthrough.mjs
node t25-versioning-walkthrough.mjs
node t23-demo-mode-walkthrough.mjs
node t22-theme-icon-walkthrough.mjs
node t13-chat-detail-regression.mjs
node t10-sidebar-actions-regression.mjs
node t9-agent-quality-regression.mjs
node t8-input-regression.mjs
node t7-workspace-regression.mjs
node browser-regression.mjs
```

最近验证结果：

- T28 Mock SSE：11/11 通过。
- T29 真实 AI 全链路：32/32 通过，总耗时约 49.5 秒。
- T29 首轮生成上游调用 1 次，增量修改保留前轮特征且未新建项目。
- 版本链路 `v1 → v2 → v3 rollback → v4 edit` 完整恢复。
- T25 版本回归：26/26 通过。
- T26 归档回归：14/14 通过。
- T27 浅色体系回归：17/17 通过。
- T9 真实 AI 全链路：14/14 通过。
- 产物 iframe JavaScript 语法错误留痕为 0。
- lint 通过，Vite 构建和预渲染通过。

## 使用流程

1. 注册或登录账户。
2. 在首页输入应用需求，可添加附件、语音或 MCP。
3. 可选选择构建类型和主题后提交。
4. 在详情页查看计划、流式输出和消息历史。
5. 在右侧预览应用或查看源码。
6. 基于当前项目提交增量修改。
7. 在版本面板切换历史版本或执行回滚。
8. 使用在线编辑器修改 HTML，保存后同步 Preview。
9. 导出当前版本 HTML。
10. 刷新详情页，确认消息、项目、版本和生成状态恢复。
11. 修改应用后在 App Viewer 中重新点击 Publish。
12. 生成失败时按提示重试或主动使用演示模式。
