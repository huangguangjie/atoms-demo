# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，由智能体生成可预览、可编辑、可保存和可发布的 HTML/JavaScript 应用，并支持会话恢复、项目管理、演示模式和版本管理。

当前产品流程为“首页输入 → 会话详情开发 → 应用预览与发布”：

- 首页创建会话并进入详情页，历史会话可恢复消息和关联项目。
- 详情页左侧展示对话、计划和 Agent 输出，右侧提供 Preview、源码查看、版本管理及 Publish。
- 支持基于已有 `app_html` 的连续增量修改，后续修改会携带上一轮产物作为上下文。
- 项目产物支持版本历史、版本切换预览、回滚、在线 HTML 编辑保存和 HTML 导出。
- 演示模式元数据持久化到消息和项目，刷新后继续显示演示标识。
- AI 网关已验证可达，但当前上游账户余额不足，真实模型请求返回 HTTP 402；该错误不会进入无意义重试或自动伪造成功。
- 真实 AI 不可用时，用户仍可显式选择演示模式验证完整产品流程。

# Project Module Description

## 工作区、认证与会话

- 支持邮箱注册、登录、退出和工作区切换。
- 注册时通过数据库触发器幂等创建用户 profile 与默认工作区。
- Supabase Auth、PostgreSQL 和 RLS 隔离用户、工作区、会话、消息及项目数据。
- 侧边栏支持折叠、窄屏布局、收藏、重命名和删除会话。
- 历史会话进入独立详情页，恢复消息、项目和项目版本。

## Chat 输入区

- 首页与详情页共用 `ChatComposer`。
- 支持文本、附件、语音转写、MCP、主题、构建模式和目标模式。
- `+` 菜单提供附件、MCP、视频、深度研究和竞赛模式入口。
- 输入 `#` 可引用上传内容、AI、密钥和项目。
- 引用菜单支持过滤、键盘导航、Escape 关闭和点击外部区域关闭。
- 输入区兼容中文输入法组合输入。
- 用户消息先更新界面，再写入数据库，避免刷新后丢失。

## 会话详情与应用查看

- 详情页采用顶部工具栏、左侧 Chat、右侧 App Viewer 的三段式布局。
- 支持 SSE 流式生成、停止生成、错误恢复、重新生成和历史消息加载。
- App Viewer 支持 iframe 预览、源码查看、在线编辑、版本面板和 Publish。
- Preview 与源码页签使用当前查看的产物保持同步。
- 项目修改后需要重新 Publish。
- 无关联项目、项目尚未落库或生成失败时显示明确空状态。

## 增量修改

- 首次生成使用普通生成流程。
- 对已有项目继续发送修改请求时，客户端传递 `previousHtml`。
- Edge Function 将上一轮 HTML 注入修改上下文，执行针对性规划和代码生成。
- 连续多轮修改均基于上一轮已落库产物，避免回退到初始版本。
- 每轮生成结果同时更新项目当前 HTML、Preview、源码和版本历史。

## 项目版本管理

- `project_versions` 保存版本号、来源、创建时间和 `app_html` 快照。
- 版本来源包括 `generation`、`edit` 和 `rollback`。
- 支持版本历史列表、版本切换查看和回滚。
- 回滚不会删除历史版本，而是创建一个新的 `rollback` 版本。
- 在线编辑器保存后会更新项目 HTML、Preview 和版本历史。
- 支持导出当前查看版本的 HTML 文件。
- 刷新后项目版本通过服务端查询恢复；版本加载链路包含异步恢复逻辑，回归脚本覆盖该场景。

## 主题与构建模式

- 主题按钮使用 `Palette` 图标，默认不选择主题。
- 未选择主题时不注入主题提示词，并使用中性色板作为预览回退。
- 明确选择主题后才会影响生成和预览，刷新后保留选择。
- 构建类型和目标模式展示名称，菜单选中项的对勾位于右侧。

## AI 生成管线

Edge Function 按以下阶段处理请求：

1. 意图识别。
2. 布局规划。
3. HTML/JavaScript 代码生成。
4. 产物清理与持久化。
5. 项目版本写入。

通过 `plan`、`message`、`step`、`app`、`done` 和 `error` SSE 事件传递生成过程。

模型与重试链路如下：

1. 瞬时失败时进行同模型重试。
2. 仍然失败时按顺序切换备用模型。
3. SSE 已开始输出后不切换模型。
4. 客户端对连接失败、5xx 和可重试的 429 最多执行三次指数退避重试。
5. 上游余额不足时返回 HTTP 402，不进行无意义重试。
6. 所有模型均失败时返回明确错误，不伪造成功状态。

当前模型配置为：

- 主模型：`deepseek-v4-flash`
- 备用模型：`gpt-5.4`
- 备用模型：`gemini-3.1-pro-preview`

## 失败处理与演示模式

- 生成失败卡提供“重新生成”入口。
- HTTP 402 会显示 AI 账户余额不足的明确提示。
- 用户可主动点击“使用演示模式生成”生成本地演示应用。
- 演示应用会正常落库，并在消息 metadata、项目字段和 App Viewer 中显示“演示模式”标识。
- 刷新历史详情后根据 `messages.metadata.isDemo` 与 `projects.is_demo` 恢复演示状态。
- 演示模式仅作为明确的产品兜底，不替代真实 AI 自动执行。

## 语音、模板与项目

- 音频通过 `app_atoms_transcribe_audio` Edge Function 转换为文本。
- 模板支持占位字段填写、体验、克隆和魔改。
- 模板输入按 HTML/JavaScript 上下文进行转义。
- 我的项目展示真实项目，支持收藏、预览、版本查看和历史回放。
- 项目通过会话关联，在详情页自动恢复。

## 博客与内容

- 支持博客索引、文章详情和 Markdown 渲染。
- 提供博客预渲染路由和站点地图生成。

## 当前外部限制

- 上游 AI 网关可达，但当前账户余额不足，真实模型请求返回 HTTP 402。
- 部分早期请求曾表现为 Edge Function HTTP 502；当前错误分类已区分余额不足和网关故障。
- 真实模型链路恢复前，可使用显式演示模式验证生成、增量修改和版本管理流程。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、运行、部署和验证说明
├── PROJECT_NOTES.md                           # 实现思路、架构设计、完成度和规划
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # AI 生成、增量修改、错误分类、SSE 和清理
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/                        # 数据库结构、触发器、关联、版本和 RLS
│   │   └── scripts/                           # 部署、SQL、调试和质量验证脚本
│   └── frontend/
│       ├── package.json                       # 前端依赖和脚本
│       ├── vite.config.ts                     # Vite 配置
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── *-regression.mjs                  # 浏览器、工作区和功能回归脚本
│       ├── t22-theme-icon-walkthrough.mjs     # 主题与错误恢复走查
│       ├── t23-demo-mode-walkthrough.mjs     # 演示模式走查
│       ├── t25-versioning-walkthrough.mjs    # 增量修改与版本管理走查
│       ├── t25-diag.mjs                      # T25 版本恢复诊断脚本
│       ├── t25-diag2.mjs                     # T25 刷新及网络请求诊断脚本
│       ├── tools/                             # 本地登录会话辅助工具
│       ├── public/                            # 静态资源
│       ├── prerender/                         # 博客预渲染和站点地图脚本
│       └── src/
│           ├── App.tsx                        # 根组件与路由布局
│           ├── main.tsx                       # 前端启动入口
│           ├── pages/
│           │   ├── HomePage.tsx               # 首页输入、主题和会话创建
│           │   └── ChatDetailPage.tsx         # 详情页、生成、增量修改和版本管理
│           ├── components/                    # 布局、聊天、预览、版本和 UI
│           ├── contexts/                      # 认证和用户数据上下文
│           ├── hooks/                         # 通用 Hooks
│           └── lib/                           # Supabase、Agent 和演示资源封装
├── docs/                                      # 系统设计、数据模型、部署和使用文档
├── assets/                                    # 测试音频等资源
├── uploads/                                   # 输入区和布局参考资源
└── .atoms/
    ├── ATOMS.md                              # 项目约束、决策与上下文
    └── PROGRESS.md                            # T1-T25 工作状态与进度日志
```

临时回归截图、诊断 JSON 和 `.tmp-*` 文件仅用于验证，不属于产品运行所需资源。

# File Description Inventory

| 文件或目录 | 用途 |
|---|---|
| `README.md` | 项目功能全景、目录、运行、部署和验证说明 |
| `PROJECT_NOTES.md` | 产品取舍、技术架构、任务完成度和后续优先级 |
| `app/frontend/package.json` | 前端依赖、脚本及 Playwright 配置 |
| `app/frontend/src/App.tsx` | 根路由及首页、详情页布局切换 |
| `app/frontend/src/pages/HomePage.tsx` | 首页输入、主题状态、项目查询和会话创建 |
| `app/frontend/src/pages/ChatDetailPage.tsx` | 详情布局、生成、增量修改、版本、编辑和演示恢复 |
| `app/frontend/src/components/chat/ChatComposer.tsx` | 输入区、菜单、引用、主题和发送控制 |
| `app/frontend/src/components/chat/ChatMessage.tsx` | 消息展示、计划卡和演示模式标识 |
| `app/frontend/src/components/layout/Sidebar.tsx` | 工作区、历史会话和会话操作 |
| `app/frontend/src/lib/agent.ts` | Edge Function 调用、SSE 解析、错误透传和客户端重试 |
| `app/frontend/src/lib/supabase.ts` | 会话、消息、项目、版本及关联查询 |
| `app/frontend/src/lib/demo-apps.ts` | 演示应用、主题色板和演示模式资源 |
| `app/frontend/src/index.css` | 全局布局和基础样式 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | AI 管线、增量上下文、模型降级、错误分类和 SSE |
| `app/backend/functions/app_atoms_transcribe_audio/index.ts` | 音频转文字 |
| `app/backend/migrations/006_project_versions_and_metadata.sql` | 项目版本表、演示元数据字段、回填、约束和 RLS |
| `app/frontend/t23-demo-mode-walkthrough.mjs` | 演示模式生成、落库和刷新回放走查 |
| `app/frontend/t25-versioning-walkthrough.mjs` | 两轮增量修改、版本切换、回滚、编辑、导出和刷新走查 |
| `app/frontend/t25-diag.mjs` | 版本恢复及 `fetchProjectVersions` 调用诊断 |
| `app/frontend/t25-diag2.mjs` | 刷新后的版本请求和页面状态诊断 |
| `app/backend/scripts/test_edge_prompt.py` | Edge Function 和 AI 提示链路验证 |
| `docs/deployment.md` | 部署、模型配置、错误分类和演示模式说明 |
| `.atoms/ATOMS.md` | 工作区决策和运行约束 |
| `.atoms/PROGRESS.md` | T25 任务条目与进度日志 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18、Vite | 单页应用和页面构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS、CSS | 页面、响应式样式和布局 |
| UI 组件 | shadcn/ui、Radix UI | 菜单、Popover、选择器和交互组件 |
| 图标 | lucide-react | 导航、主题和操作图标 |
| 路由 | React Router | 首页与会话详情导航 |
| 状态与数据 | React Context、Supabase | 认证、工作区、会话、消息、项目和版本 |
| 后端平台 | Supabase Auth、PostgreSQL、RLS、Edge Functions | 认证、数据隔离和服务端逻辑 |
| 智能体 | DeepSeek、GPT、Gemini、AI 网关、SSE | 规划、代码生成、增量修改、降级和流式传输 |
| Edge Function | Deno、TypeScript | AI 调用、重试、错误分类、超时和产物清理 |
| 语音转写 | `scribe_v2`、AI 网关 | 音频转文字 |
| 外部连接 | MCP | 远程工具和工作区连接 |
| 内容处理 | Markdown、预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright 1.52.0、Node、Python | 浏览器回归、诊断、质量门禁和 E2E |
| 代码规范 | ESLint、TypeScript strict | 静态检查和类型校验 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。演示模式使用本地演示资源，不作为云端 AI 的静默替代。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用：

```bash
npm install
```

Playwright 应使用项目锁定版本 1.52.0。

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置，不要提交 `.env.local`。

在后端配置 AI 网关和密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

## 初始化数据库

按迁移顺序执行：

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
python scripts/run_sql.py migrations/004_user_init_trigger.sql
python scripts/run_sql.py migrations/005_conversation_actions.sql
python scripts/run_sql.py migrations/006_conversation_project.sql
python scripts/run_sql.py migrations/006_project_versions_and_metadata.sql
```

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

确认应用生成和音频转写函数已发布，并配置服务端 Secrets。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

未配置 Supabase 或真实 AI 网关时，可使用演示资源和用户主动触发的演示模式。真实 AI 余额不足时会明确返回 402，不会自动切换到演示模式。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
pnpm preview
```

当前质量门禁：

- `pnpm lint`：通过，0 个错误。
- Vite build：通过。
- 已包含博客首页和博客路由预渲染。
- T22、T23、T25 及 T7-T13 回归脚本可按需执行。

## 回归验证

```bash
cd app/frontend
node t25-versioning-walkthrough.mjs
node t23-demo-mode-walkthrough.mjs
node t22-theme-icon-walkthrough.mjs
node t13-chat-detail-regression.mjs
node t10-sidebar-actions-regression.mjs
node t9-agent-quality-regression.mjs
node t8-input-regression.mjs
node t7-workspace-regression.mjs
node browser-regression.mjs
```

版本刷新问题可使用以下诊断脚本：

```bash
cd app/frontend
node t25-diag.mjs
node t25-diag2.mjs
```

平台 AI 余额恢复后，应依据 `docs/deployment.md` 和后端 E2E 脚本执行真实 AI 全链路复测，并记录时间、耗时、SSE 事件序列和提交 SHA。

## 使用流程

1. 注册或登录账户。
2. 确认 profile 和默认工作区已创建。
3. 切换工作区或创建新工作区。
4. 在首页输入应用需求，可添加附件、语音或 MCP。
5. 输入 `#` 并选择引用内容。
6. 可选选择构建类型和主题。
7. 提交需求并进入会话详情页。
8. 在左侧查看计划、流式输出和消息历史。
9. 在右侧预览应用或查看源码。
10. 基于当前项目继续提交修改，生成增量版本。
11. 在版本面板切换历史版本并预览。
12. 需要恢复旧版本时执行回滚，回滚会创建新版本。
13. 使用在线编辑器修改 HTML，保存后同步 Preview。
14. 导出当前版本 HTML。
15. 生成失败或余额不足时，按提示使用“使用演示模式生成”。
16. 刷新历史详情，确认消息、项目描述和查看器中的演示模式标识仍一致。
17. 修改应用后重新 Publish。
