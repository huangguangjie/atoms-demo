# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，选择工作区、构建模式和可选主题，并使用附件、语音、引用及 MCP 等能力，由智能体生成可预览、保存和发布的应用。

当前产品形成“首页输入 → 会话详情开发 → 应用查看与发布”的完整流程：

- 首页提交需求后创建会话并进入详情页。
- 历史会话点击后整体进入独立详情页，不再保留全局导航菜单。
- 详情页左侧完整回放会话消息，右侧自动加载关联应用。
- 用户消息、智能体计划、流式输出和最终结果均可持久化并刷新恢复。
- 主题默认不选择任何项目；用户明确选择后才会影响生成和预览。
- 输入框输入 `#` 时弹出引用菜单，支持上传、AI、密钥和项目。
- 构建类型及同类菜单的选中标记统一显示在行右侧。
- 侧边栏历史会话支持收藏、重命名和删除。
- AI 网关瞬时 5xx 错误支持客户端与服务端重试，并在最终失败时保留真实错误信息。
- App Viewer 支持应用预览、源码查看和发布。

# Project Module Description

## 应用布局与导航

- 首页使用完整工作区布局，提供侧边栏、欢迎状态和输入区。
- 详情页使用独立三段式布局：顶部工具栏、左侧聊天区、右侧应用查看器。
- 详情页保留 Logo 返回首页和历史会话切换入口，但不显示首页、资源和项目等全局菜单。
- 历史会话点击后直接进入对应详情页，不残留首页视图。
- 侧边栏支持折叠、窄屏布局及会话操作菜单。

## 认证与工作区

- 支持邮箱注册、登录和退出。
- 注册时通过数据库触发器幂等创建 profile 和默认工作区。
- Supabase Auth 与 RLS 隔离用户、工作区、会话、消息和项目数据。
- 登录后自动加载工作区，并按工作区筛选会话。
- 受保护操作在未登录时显示认证提示。

## 首页输入与对话详情

- 首页负责欢迎状态、需求输入、会话创建和跳转。
- 详情页负责历史消息加载、流式生成、停止生成和应用结果查看。
- 输入区支持文本、附件、`#` 引用、MCP、语音转写、主题、构建模式、发送和停止。
- 历史详情按时间顺序完整展示用户消息、计划卡、Agent 输出和最终结果。
- 新提交的用户消息会先更新界面，再写入 `messages`，保证刷新后可回放。
- 会话通过 `conversation_id` 与项目关联，详情页刷新后自动恢复关联应用。
- 无关联应用时，右侧查看器显示合理空状态。

## 输入区与引用菜单

- `+` 面板保留附件、MCP、视频、深度研究和竞赛模式等入口，不再提供 `#` 引用入口。
- 在输入框输入 `#` 时自动显示浮层菜单，选项包括上传、AI、密钥和项目。
- 输入后续字符可过滤选项；删除 `#`、按 Escape 或点击外部区域可关闭菜单。
- 项目选项沿用现有引用注入逻辑并生成引用 chip。
- 上传选项打开附件上传流程。
- AI 和密钥选项提供对应引用或快捷配置交互。
- 菜单支持键盘导航，且不干扰中文输入法组合输入。
- 首页和详情页共用同一套 ChatComposer 行为。

## 主题与构建模式

- 主题默认值为未选择状态，菜单打开时不高亮任何主题。
- 未选择主题时，生成链路不注入主题提示词，并使用中性色板作为预览回退。
- 用户明确选择主题后，主题才会高亮、持久化，并作用于应用生成和预览。
- 刷新后保留用户已经选择的主题，不会自动恢复为预设主题。
- 构建类型和目标模式菜单的触发框只显示名称，不显示图标。
- 菜单选中项的对勾位于行右侧，未选中项不保留错误占位。

## 智能体生成链路

- Edge Function 按“意图识别 → 布局规划 → 代码约束生成 → 产物清理”处理请求。
- 生成结果通过 `plan`、`message`、`step`、`app`、`done` 和 `error` SSE 事件传递。
- 代码生成强调 Flex/Grid、响应式断点和受控定位。
- 主题仅在用户明确选择后注入生成提示。
- 服务端对 AI 网关 5xx 错误进行退避重试，客户端对瞬时连接失败和 5xx 再重试一次。
- 生成失败时不伪造成功状态，向前端透出可识别的真实错误。
- 生成输出会清理 Markdown、代码围栏和额外说明，并限制超时、输出长度和重试次数。

## 应用查看器与发布

- 详情页右侧自动加载关联项目并显示 iframe 预览。
- 支持应用源码查看。
- 生成 HTML 保存到 `projects.app_html`。
- Publish 生成发布版本；应用修改后需要重新发布。
- 概览、编辑器、Atoms 云、文件、增长、终端、计划器、浏览器和记事本等标签仍有部分占位实现。

## 资源、模板与项目

- 资源页展示社区应用并支持分类筛选。
- 模板支持占位字段填写、体验、克隆和魔改，并按 HTML/JavaScript 上下文转义输入。
- 我的项目展示真实项目，支持收藏、预览和历史回放。
- 项目可通过会话关联，在详情页自动恢复并展示。

## 博客与内容

- 支持博客索引、文章详情和 Markdown 渲染。
- 提供博客预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、开发与部署入口
├── PROJECT_NOTES.md                           # 实现取舍、完成度与后续规划
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # 智能体生成、主题处理、重试与清理
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/                        # 数据库结构、关联、触发器和 RLS 迁移
│   │   └── scripts/                           # 部署、SQL、调试和质量验证脚本
│   └── frontend/
│       ├── package.json                       # 前端依赖与脚本
│       ├── vite.config.ts                     # Vite 配置
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── *-regression.mjs                  # 浏览器、工作区、输入和详情页回归脚本
│       ├── tools/                             # 本地登录会话辅助工具
│       ├── public/                            # 静态资源
│       ├── prerender/                         # 博客预渲染和站点地图脚本
│       └── src/
│           ├── App.tsx                        # 根组件与路由布局
│           ├── main.tsx                       # 前端启动入口
│           ├── pages/
│           │   ├── HomePage.tsx               # 首页输入、主题和会话创建
│           │   └── ChatDetailPage.tsx         # 详情布局、历史回放和应用查看
│           ├── components/                    # 认证、布局、聊天、预览、模板、博客和 UI
│           ├── contexts/                      # 认证和用户数据上下文
│           ├── hooks/                          # 通用 Hooks
│           └── lib/                            # Supabase、智能体和演示资源封装
├── docs/                                      # 系统设计、数据模型、部署和使用文档
├── assets/                                    # 测试音频等资源
├── uploads/                                   # 输入区和布局参考截图
└── .atoms/
    ├── ATOMS.md                              # 项目约束与上下文
    └── PROGRESS.md                            # T1-T19 任务状态与进度日志
```

临时回归截图 `.tmp-t7-*`、`.tmp-t8-*` 和 `.tmp-t13-*` 仅用于验证，不属于产品运行所需资源。

# File Description Inventory

| 文件或目录 | 类型 | 用途 |
|---|---|---|
| `PROJECT_NOTES.md` | Markdown | 记录架构取舍、完成度和后续优先级 |
| `app/frontend/src/App.tsx` | TSX | 根路由及首页、详情页布局切换 |
| `app/frontend/src/pages/HomePage.tsx` | TSX | 首页输入、主题状态、项目引用和会话创建 |
| `app/frontend/src/pages/ChatDetailPage.tsx` | TSX | 独立详情布局、消息回放、项目恢复和流式生成 |
| `app/frontend/src/components/chat/ChatComposer.tsx` | TSX | 输入区、主题、附件、MCP、`#` 浮层和发送控制 |
| `app/frontend/src/components/layout/Sidebar.tsx` | TSX | 工作区、历史会话、导航和会话操作 |
| `app/frontend/src/lib/agent.ts` | TypeScript | Edge Function 调用、SSE 解析、超时和错误重试 |
| `app/frontend/src/lib/supabase.ts` | TypeScript | 会话、消息、项目及会话项目关联查询 |
| `app/frontend/src/lib/demo-apps.ts` | TypeScript | 演示应用、主题色板和中性回退 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | TypeScript | AI 管线、主题提示、SSE 输出和 HTML 清理 |
| `app/backend/functions/app_atoms_transcribe_audio/index.ts` | TypeScript | 音频转文字 |
| `app/backend/migrations/006_conversation_project.sql` | SQL | 会话与项目关联结构及约束 |
| `app/frontend/src/components/ui/dropdown-menu.tsx` | TSX | 下拉菜单及右侧选中标记 |
| `app/frontend/src/components/ui/select.tsx` | TSX | 选择菜单及右侧选中标记 |
| `app/frontend/src/components/ui/menubar.tsx` | TSX | 菜单栏通用交互 |
| `app/frontend/src/components/ui/context-menu.tsx` | TSX | 上下文菜单通用交互 |
| `app/frontend/t7-workspace-regression.mjs` | JavaScript | 工作区、返回首页和会话流程回归 |
| `app/frontend/t8-input-regression.mjs` | JavaScript | 输入区、主题、引用和菜单回归 |
| `app/frontend/t13-chat-detail-regression.mjs` | JavaScript | 详情页布局、历史回放和应用展示回归 |
| `app/backend/scripts/test_edge_prompt.py` | Python | Edge Function 及 AI 提示链路验证 |
| `.atoms/PROGRESS.md` | Markdown | T15-T19 完成状态、回归结果和发布记录 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18、Vite | 单页应用和页面构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS | 页面和响应式样式 |
| UI 组件 | shadcn/ui、Radix UI | 菜单、Popover、选择器和交互组件 |
| 图标 | lucide-react | 导航和操作图标 |
| 路由 | React Router | 首页与会话详情导航 |
| 状态与数据 | React Context、Supabase | 认证、工作区、会话、消息和项目 |
| 后端平台 | Supabase Auth、PostgreSQL、RLS、Edge Functions | 认证、数据隔离和服务端逻辑 |
| 智能体 | `deepseek-v4-flash`、AI 网关、SSE | 意图识别、规划和代码生成 |
| Edge Function | Deno、TypeScript | AI 调用、重试、超时和产物清理 |
| 语音转写 | `scribe_v2`、AI 网关 | 音频转文字 |
| 外部连接 | MCP | 远程工具和工作区连接 |
| 内容处理 | Markdown、预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright、Node、Python | 浏览器回归、质量门禁和 E2E |
| 代码规范 | ESLint、TypeScript strict | 静态检查和类型校验 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用：

```bash
npm install
```

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置，不要提交 `.env.local`。

在后端配置 AI 网关和密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

## 初始化数据库

按迁移顺序执行：

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
python scripts/run_sql.py migrations/004_user_init_trigger.sql
python scripts/run_sql.py migrations/005_conversation_actions.sql
python scripts/run_sql.py migrations/006_conversation_project.sql
```

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

确认应用生成和音频转写函数已发布，并配置服务端 Secrets。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

未配置 Supabase 时可使用演示资源和本地演示智能体；完成配置后使用真实认证、数据和 AI 生成链路。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
pnpm preview
```

## 回归验证

```bash
cd app/frontend
node t13-chat-detail-regression.mjs
node t10-sidebar-actions-regression.mjs
node t9-agent-quality-regression.mjs
node t8-input-regression.mjs
node t7-workspace-regression.mjs
node browser-regression.mjs
```

当前质量门禁结果：

- lint：0 个错误。
- build：成功。
- T7：15/15 通过。
- T8：22/22 通过。
- T10：20/20 通过。
- T13：17/17 通过。
- T9 已保留既有质量验证结果；回归期间 AI 上游网关的 403/502 属于外部服务故障，未影响会话和消息落库。

## 使用流程

1. 注册或登录账户。
2. 确认 profile 和默认工作区已创建。
3. 切换工作区或创建新工作区。
4. 在首页输入应用需求，可添加附件、语音或 MCP。
5. 如需引用，在输入框输入 `#` 并选择上传、AI、密钥或项目。
6. 可选选择构建类型和主题；未选择主题时直接使用中性默认处理。
7. 提交需求后进入对话详情页。
8. 在左侧查看完整计划、流式输出和消息历史。
9. 在右侧自动预览关联应用或查看源码。
10. 从详情页返回首页，或通过历史会话切换其他会话。
11. 修改应用后重新 Publish 以发布最新版本。
