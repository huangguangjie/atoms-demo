# Project Summary

Atoms 是面向开发者的智能体驱动应用生成平台。用户通过自然语言描述需求，选择工作区、主题和构建模式，并使用附件、语音、引用及 MCP 等输入能力，由智能体生成可预览、保存和发布的应用。

当前产品形成“首页输入 → 会话详情开发 → 应用查看与发布”的完整流程：

- 首页提交需求后创建会话并进入 `/chat/:conversationId`。
- 对话详情页支持历史消息回放、流式生成、停止生成和应用预览。
- 用户消息会持久化到数据库，刷新后可恢复会话历史。
- AI 网关瞬时 5xx 错误支持客户端与服务端重试，并在最终失败时保留真实错误信息。
- 侧边栏历史会话支持收藏、重命名和删除。
- 主题、附件、MCP、语音和构建模式等输入能力继续接入生成链路。
- App Viewer 当前支持应用预览和源码查看，其他页面标签仍以占位功能为主。

# Project Module Description

## 应用布局与导航

- 左侧侧边栏提供 Logo、工作区、首页、资源、我的项目和最近会话。
- 最近会话按工作区过滤，点击后进入详情页并回放历史消息。
- 会话悬停菜单支持收藏、重命名和删除，操作真实写入数据库并即时刷新。
- 侧边栏支持折叠和窄屏布局。

## 认证与工作区

- 支持邮箱注册、登录和退出。
- 注册时通过数据库触发器幂等创建 profile 和默认工作区。
- Supabase Auth 与 RLS 隔离用户、工作区、会话、消息和项目数据。
- 登录后自动加载工作区，并按工作区筛选会话。

## 首页输入与对话详情

- 首页负责欢迎状态、需求输入、会话创建和跳转。
- 详情页负责历史加载、智能体生成、消息展示和应用结果查看。
- 输入区支持文本、附件、`#` 引用、MCP、语音转写、主题、构建模式、发送和停止。
- 后续提交的用户消息会先更新界面，再真实写入 `messages`，保证刷新后可回放。
- 生成过程中支持停止；空闲时显示发送操作。

## 输入区与主题

- `+` 面板采用深色分组布局，展开时图标旋转 45 度，支持外部点击和 Escape 关闭。
- 面板保留团队模式、附件、连接器/MCP、视频、深度研究和竞赛模式入口。
- 主题面板支持搜索、选择和预览，选择结果会传入生成请求并影响应用视觉风格。
- 构建类型选择框仅显示文本名称，不显示图标。

## 智能体生成链路

- Edge Function 按“意图识别 → 布局规划 → 代码约束生成 → 产物清理”处理请求。
- 生成结果通过 `plan`、`message`、`step`、`app`、`done` 和 `error` SSE 事件传递。
- 代码生成强调 Flex/Grid、响应式断点和受控定位。
- 服务端对 AI 网关 5xx 错误进行一次退避重试；客户端对瞬时连接失败和 5xx 再重试一次。
- 失败时不伪造成功状态，向前端透出可识别的真实错误。
- 生成输出会清理 Markdown、代码围栏和额外说明，并限制超时、输出长度和重试次数。

## 应用查看器与发布

- 详情页右侧展示应用查看器，支持 iframe 预览和源码查看。
- 生成 HTML 保存到 `projects.app_html`。
- Publish 生成发布版本；应用修改后需要重新发布。
- 概览、编辑器、Atoms 云、文件、增长、终端、计划器、浏览器和记事本等标签当前仍有 TODO 或占位实现。

## 资源、模板与项目

- 资源页展示社区应用并支持分类筛选。
- 模板支持占位字段填写、体验、克隆和魔改，并按 HTML/JavaScript 上下文转义输入。
- 我的项目展示真实项目，支持收藏、预览和历史回放。
- 未登录执行受保护操作时显示认证提示。

## 博客与内容

- 支持博客索引、文章详情和 Markdown 渲染。
- 提供博客预渲染路由和站点地图生成。

# Directory Tree

```text
.
├── README.md                                  # 项目总览、开发与部署入口
├── PROJECT_NOTES.md                           # 实现取舍、完成度与后续规划说明
├── app/
│   ├── backend/
│   │   ├── README.md                          # 后端与 Edge Function 配置说明
│   │   ├── skills_docs/                       # Supabase 开发规范和函数说明
│   │   ├── functions/
│   │   │   ├── app_atoms_agent_generate/
│   │   │   │   └── index.ts                   # 意图识别、布局规划、生成、重试与清理
│   │   │   └── app_atoms_transcribe_audio/
│   │   │       └── index.ts                   # 音频转写 Edge Function
│   │   ├── migrations/                        # 数据库结构、约束、触发器和 RLS 迁移
│   │   └── scripts/                           # 部署、SQL、调试和质量验证脚本
│   └── frontend/
│       ├── package.json                        # 前端依赖与脚本
│       ├── vite.config.ts                     # Vite 配置
│       ├── tailwind.config.ts                 # Tailwind 配置
│       ├── components.json                    # shadcn/ui 配置
│       ├── site.config.json                   # 站点配置
│       ├── template_config.json               # 模板配置
│       ├── *-regression.mjs                  # 浏览器、工作区、输入、智能体和详情页回归脚本
│       ├── tools/                             # 本地登录会话辅助工具
│       ├── public/                            # 静态资源
│       ├── prerender/                         # 博客预渲染和站点地图脚本
│       └── src/
│           ├── App.tsx                        # 根组件与路由
│           ├── main.tsx                       # 前端启动入口
│           ├── pages/
│           │   ├── HomePage.tsx               # 首页欢迎状态和输入提交
│           │   └── ChatDetailPage.tsx         # 对话详情、流式开发和应用查看
│           ├── components/                    # 认证、布局、聊天、预览、模板、博客和 UI
│           ├── contexts/                      # 认证和用户数据上下文
│           ├── hooks/                          # 通用 Hooks
│           └── lib/                            # Supabase、智能体调用和演示资源封装
├── docs/                                      # 系统设计、数据模型、部署和使用文档
├── assets/                                    # 测试音频等资源
├── uploads/                                   # 输入区和布局参考截图
└── .atoms/
    ├── ATOMS.md                              # 项目约束与上下文
    └── PROGRESS.md                            # T1-T14 任务状态与进度日志
```

临时回归截图 `.tmp-t13-error.png` 和 `.tmp-t13-final.png` 不属于产品运行所需资源。

# File Description Inventory

| 文件或目录 | 类型 | 用途 |
|---|---|---|
| `PROJECT_NOTES.md` | Markdown | 记录架构取舍、完成度和后续优先级 |
| `app/frontend/src/App.tsx` | TSX | 应用路由与页面入口 |
| `app/frontend/src/pages/HomePage.tsx` | TSX | 首页输入、会话创建和跳转 |
| `app/frontend/src/pages/ChatDetailPage.tsx` | TSX | 历史回放、消息落库、流式生成和应用查看 |
| `app/frontend/src/components/chat/ChatComposer.tsx` | TSX | 输入区、附件、引用、MCP、主题和 `+` 面板 |
| `app/frontend/src/components/layout/Sidebar.tsx` | TSX | 工作区、会话列表、操作菜单和导航 |
| `app/frontend/src/lib/agent.ts` | TypeScript | Edge Function 调用、SSE 解析、超时和瞬时错误重试 |
| `app/frontend/t13-chat-detail-regression.mjs` | JavaScript | 对话详情页跳转、生成、停止、落库、回放和窄屏回归 |
| `app/frontend/src/lib/supabase.ts` | TypeScript | 认证、会话查询和会话操作封装 |
| `app/frontend/src/lib/demo-apps.ts` | TypeScript | 演示应用与主题色板映射 |
| `app/backend/functions/app_atoms_agent_generate/index.ts` | TypeScript | AI 管线、网关重试、SSE 输出和 HTML 清理 |
| `app/backend/migrations/001_initial_schema.sql` | SQL | 初始化业务表、RLS 和公共种子数据 |
| `app/backend/migrations/005_conversation_actions.sql` | SQL | 会话收藏字段和排序索引 |
| `app/frontend/src/components/preview/` | TSX | iframe 应用预览和源码查看 |
| `app/frontend/src/contexts/AuthContext.tsx` | TSX | 认证、profile 和工作区管理 |
| `app/backend/scripts/` | Python/SQL | 数据库执行、部署、调试和端到端验证 |
| `docs/usage.md` | Markdown | 首页提交、详情页开发和发布流程 |
| `docs/deployment.md` | Markdown | 部署、质量门禁和回归脚本说明 |
| `.atoms/PROGRESS.md` | Markdown | T1-T14 任务与验证记录 |

# Technology Stack

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18、Vite | 单页应用和页面构建 |
| 语言 | TypeScript | 类型安全开发 |
| 样式 | Tailwind CSS | 页面和响应式样式 |
| UI 组件 | shadcn/ui、Radix UI | 弹窗、Popover、菜单和交互组件 |
| 图标 | lucide-react | 导航和操作图标 |
| 路由 | React Router | 首页与对话详情导航 |
| 状态与数据 | React Context、Supabase | 认证、工作区、会话、消息和项目 |
| 后端平台 | Supabase Auth、PostgreSQL、RLS、Edge Functions | 认证、数据隔离和服务端逻辑 |
| 智能体 | `deepseek-v4-flash`、AI 网关、SSE | 意图识别、规划和代码生成 |
| Edge Function | Deno、TypeScript | AI 调用、重试、超时和产物清理 |
| 语音转写 | `scribe_v2`、AI 网关 | 音频转文字 |
| 外部连接 | MCP | 远程工具和工作区连接 |
| 内容处理 | Markdown、预渲染脚本 | 博客展示和静态生成 |
| 测试 | Playwright、Node、Python | 浏览器回归、质量门禁和 E2E |
| 代码规范 | ESLint、TypeScript strict | 静态检查和类型校验 |

AI 密钥仅配置在 Supabase Edge Function Secrets 中，不暴露给浏览器。

# Usage

## 安装依赖

```bash
cd app/frontend
pnpm install
```

也可使用：

```bash
npm install
```

## 配置环境变量

在 `app/frontend` 创建本地环境配置，填写 Supabase 项目地址、匿名密钥及前端配置，不要提交 `.env.local`。

在后端配置 AI 网关和密钥：

```bash
cd app/backend
python scripts/set_secrets.py
```

## 初始化数据库

按迁移顺序执行：

```bash
cd app/backend
python scripts/run_sql.py migrations/001_initial_schema.sql
python scripts/run_sql.py migrations/002_add_app_html.sql
python scripts/run_sql.py migrations/002_template_placeholders.sql
python scripts/run_sql.py migrations/003_space_default_unique.sql
python scripts/run_sql.py migrations/004_user_init_trigger.sql
python scripts/run_sql.py migrations/005_conversation_actions.sql
```

## 部署 Edge Functions

```bash
cd app/backend
python scripts/deploy_function.py
```

确认应用生成和音频转写函数已发布，并配置服务端 Secrets。

## 启动前端

```bash
cd app/frontend
pnpm dev
```

未配置 Supabase 时可使用演示资源和本地演示智能体；完成配置后使用真实认证、数据和 AI 生成链路。

## 类型检查与构建

```bash
cd app/frontend
pnpm lint
pnpm build
pnpm preview
```

## 回归验证

```bash
cd app/frontend
node t13-chat-detail-regression.mjs
node t10-sidebar-actions-regression.mjs
node t9-agent-quality-regression.mjs
node t8-input-regression.mjs
node t7-workspace-regression.mjs
node browser-regression.mjs
```

当前前端 lint 和 build 已通过。T13 详情页回归共 17 项，15 项通过；未通过项目源于测试窗口内 AI 网关的外部 403/502 瞬时故障，客户端、服务端重试和失败降级逻辑已生效。

## 使用流程

1. 注册或登录账户。
2. 确认 profile 和默认工作区已创建。
3. 切换工作区或创建新工作区。
4. 在首页输入应用需求，可添加附件、引用、语音或 MCP。
5. 选择构建类型和主题。
6. 提交需求后进入对话详情页。
7. 在左侧查看计划、流式输出和消息历史。
8. 在右侧预览应用或查看源码。
9. 在侧边栏管理历史会话。
10. 修改应用后重新 Publish 以发布最新版本。
