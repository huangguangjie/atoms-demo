# PROJECT_NOTES — 类 Atoms 全栈应用 · 简要说明

> 面向评审的一页式说明:实现思路与关键取舍、当前完成程度、后续扩展与优先级。
> 详细文档见 `docs/`(系统设计 / 数据模型 / 部署 / 使用说明),任务级进度明细见 `.atoms/PROGRESS.md`。

## 一、实现思路与关键取舍

### 1. 总体架构
- **前端 SPA**:React 18 + TypeScript + Vite + Tailwind + shadcn/ui + react-router v6;布局用 react-resizable-panels 实现可拖拽、可折叠侧边栏 + 主内容区。
- **后端 Supabase**(项目 pofchtyjqwevchiiqags):Auth(邮箱注册/登录)、Postgres + 全表 RLS、Edge Functions(AI 生成 SSE、语音转写)、模板与社区应用种子数据。用户体系复用 `auth.users`,注册触发器自动创建 profile 与默认工作区(迁移 004/005),不自建用户表或第二套登录。
- **数据访问**:统一收敛在 `src/lib/supabase.ts`(实体 CRUD + 演示兜底);云端模式下写操作与查询错误显式抛出,由调用侧 toast 透出,不做静默吞错。

### 2. AI 生成管线(Edge Function `app_atoms_agent_generate`)
四段式管线,替代"一个提示词直出 HTML"的粗放做法:
1. **意图识别 analyzeIntent**:提炼应用类型 / 内容分区 / 关键功能,模糊需求注入默认假设;
2. **布局规划 PLAN_SYSTEM**:独立小调用产出 5 步计划(布局结构 / Flex-Grid / 视觉规范 / 交互 / 响应式),15s 预算,失败回退启发式计划;
3. **代码硬约束 CODE_SYSTEM**:禁无约束绝对定位、统一间距圆角、强制移动端适配、纯 HTML 文档输出、行数约束;
4. **产物净化 sanitizeHtml**:剥离 Markdown 围栏与杂文,保证 iframe `srcDoc` 可直接运行。
- **SSE 事件协议**:`message / plan / step-* / code-delta / app / done / error`,前端渲染计划卡与代码进度;输出截断时自动以精简约束重试一次。
- **模型取舍**:主力 `deepseek-v4-flash`(复杂提示词 20–55s 稳定产出);`claude-opus-5` 质量更高但复杂提示词约 142s 超时截断——速度与稳定性优先。AI 密钥仅存 Supabase Secrets,前端不持有。

### 3. 关键决策与理由
| 决策 | 取舍逻辑 |
|------|----------|
| 单文件 HTML 应用产物 | iframe srcDoc + sandbox 直接运行,可落库、可回放、可克隆;多文件工程化生成明确列为后续项 |
| 演示模式兜底 | 未配置 Supabase 环境变量时用内存数据保证 UI 可完整预览;云端模式禁止静默回退(错误显式透出),避免掩盖线上故障 |
| 主题真实生效 | 主题名 + 中文主题指令注入生成提示词,预览色板与 AI 产物一致,而非仅前端换肤 |
| 会话归属工作区 | conversations 按 space 隔离(T7),Sidebar 最近对话 / 新建会话均按当前工作区过滤 |
| T13 提交即进入 | 首页提交后立即创建会话并跳转 `/chat/:conversationId`,生成 / 流式 / 预览 / 队列全部收敛在详情页;首页保持轻量欢迎视图,避免双视图状态机复杂化 |
| 队列续跑 | 详情页生成中提交自动入队,当前生成结束后自动执行,输入不丢失 |

## 二、当前完成程度

### 已完成(T1–T10 全部,T11/T12/T13 主体)
- **基础与数据**:布局与侧边栏(拖拽 / 折叠 / 空间选择器)、首页 / 资源页 / 项目页;真实邮箱认证、注册触发器默认工作区;会话 / 消息 / 项目 / 收藏 / 重命名 / 删除真实落库,全表 RLS。
- **AI 与语音**:Edge Function AI 生成(SSE 流式、计划卡、代码进度、应用预览)+ scribe_v2 语音转写(录音上传,失败回退浏览器识别)。
- **T7 工作区隔离**:默认工作区触发器、新建 / 切换、会话按工作区隔离(浏览器回归 16 项通过)。
- **T8 输入区重构**:大圆角卡片、附件 / # 引用 / MCP 收纳至 + 菜单、主题菜单、构建 / 目标模式、语音按钮(回归 17 项通过)。
- **T9 生成质量**:四段管线落地,5 类提示词(游戏 / 工具 / 数据 / 内容 / 复合三分区)全部通过质量门槛(19.7–52.5s);浏览器全链路回归 14/14。
- **T10 会话操作**:收藏 / 行内重命名 / 删除二次确认 / 星标排序 / 级联删除 / 服务端持久化核验(回归 20/20)。
- **T11 加号菜单**:分组面板(团队模式 / 附件 / 连接器 / 视频 / 深度研究 / 竞赛模式)、旋转 ×、Esc 与外点关闭——已实现,收尾走查中。
- **T12 主题菜单**:搜索 + 七默认主题 + 色块 + 预览卡片,主题进入真实 AI / SSE 生成链路——已实现,回归收尾中。
- **T13 详情页**:`/chat/:conversationId` 路由与页面、提交即跳转、Sidebar 历史直达、深色对话栏、生成队列、应用查看器(概览 = iframe 预览 + 源码 / 编辑器 = 只读源码 / 云·文件·增长 = 空态)——代码已落地,lint + build 通过,浏览器回归进行中。

### 未完成 / TODO
- **T13 收尾**:详情页浏览器回归(提交跳转 / 流式 / 页签切换 / 面板折叠 / 历史回放 / 窄屏)与文档更新。
- **详情页页签真实化**:Atoms 云 / 文件 / 增长为空态占位,编辑器只读;终端 / 计划器 / 浏览器 / 记事本仅在"更多"菜单占位;历史版本、分享落地页未实现。
- **工程化与深度集成**:多文件工程生成、项目工作区(项目页点击进入编辑)、附件内容进入生成上下文(当前仅记录文件名)、MCP 真实协议对接(当前本地配置 + 状态徽标)。
- **杂音排查**:community_apps 401 探针、模板自带 `/api/config` 的预期负路径 500。

## 三、继续投入的扩展方向与优先级

| 优先级 | 方向 | 理由 |
|--------|------|------|
| P0 | T13 回归收尾 + 文档 + 发布走查 | 主线交付闭环,当前唯一阻塞发布的事项 |
| P1 | 生成质量 / 速度迭代 | 更优模型 A/B、规划与代码生成并行化、"增量修改"替代整页重生成——直接决定核心体验与等待成本 |
| P2 | 详情页页签真实化 + 项目工作区 | 编辑器(只读→可编辑)、文件树、Atoms 云资源绑定,把"生成器"升级为"可持续开发的工作区" |
| P3 | 工程化多文件生成 + 分享落地页 | 支撑真实项目形态与传播;依赖 P2 的项目模型 |
| P4 | 测试 CI 化 + 部署体验 | 回归脚本容器化 / 定时执行;一键发布与预览环境 |

**总体判断**:产品主链路(输入 → 生成 → 预览 → 落库 → 回放 → 管理)已真实闭环。后续投入应优先集中在"生成质量与响应速度"和"详情页工作区化"两件事上,而非横向铺新入口。
