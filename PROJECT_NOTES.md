# PROJECT_NOTES — 类 Atoms 全栈应用 · 实现笔记

> 面向评审与后续迭代的说明:实现思路与关键取舍、架构设计、当前功能完成度(T1–T25)、计划功能优先级排期。
> 详细文档见 `docs/`(系统设计 / 数据模型 / 部署 / 使用说明),任务级进度明细见 `.atoms/PROGRESS.md`。

## 一、实现思路与关键取舍

### 1. 产品定位
还原 Atoms 官网产品形态的智能体应用平台:输入创意 → 分步计划 → 流式生成 → 实时预览 → 落库回放 → 项目管理。产物为可直接运行的单文件 HTML 应用(iframe srcDoc + sandbox),会话/项目全量持久化于 Supabase;产品价值核心在「从一句话到可运行应用」的端到端闭环,以及故障场景下的透明与可控。

### 2. 关键取舍
| 决策 | 取舍逻辑 |
|------|----------|
| 云端模式禁止静默回退(T6 教训) | 已发布版「创建对话失败,请重试」的根因是数据层静默吞错 + 未登录回退字符串 uid + Agent 失败静默回退演示智能体。修复后:数据层/Edge Function 错误显式抛出并 toast 透出,未登录先弹登录框——兜底会掩盖线上故障 |
| AI 失败故障透明 + 可重试(T21) | 失败卡展示真实错误文案与指引,「重新生成」原地续跑(不重复落库用户消息);前端瞬时故障 3 次指数退避自动重试,不把瞬时波动直接抛给用户 |
| 显式演示模式(T23) | 演示内容仅由用户在失败卡主动点击「使用演示模式生成」触发,绝不作为静默回退;消息带【演示模式】标识、查看器显示徽标、项目描述含标记——演示产物可辨识、可追溯,不掩盖真实故障 |
| 主备模型通道(T23) | 主模型 deepseek-v4-flash(复杂提示词 20–55s 稳定产出),备用 gpt-5.4、gemini-3.1-pro-preview 按序降级;单模型瞬时错误先同模型重试再切换通道——单点供应商故障不再直接导致生成失败 |
| 增量修改协议 previousHtml(T25) | 前端透传当前版本完整 HTML,服务端检测到有效 HTML(含 `</html>`)即跳过规划小调用、以「在原文基础上修改,除本次修改点外保持原样」约束生成——避免整页重生成导致既有功能丢失 |
| 版本快照四时机 + 回滚新建版本(T25) | 首次生成/增量生成/在线编辑/回滚统一写 project_versions(source=generation/edit/rollback);回滚不改写历史,以目标内容新建版本——快照不可变、可审计,UNIQUE(project_id,version_number) 兜底并发 |
| 402 确定性错误不降级(T25) | 上游「AI 账户余额不足」属账户态问题,重试与切换备用模型均无效;归类为确定性错误直接透出专属文案,避免无意义降级掩盖根因 |
| 产物交付即收敛的终止守卫(T28) | 「是否重跑」以**产物是否已交付(app 事件)**为唯一判据:app 到手后 SSE 缺 done 分片/连接截断一律补发 done 收敛,绝不整轮重跑(重新规划→重新生成);仅未交付产物的瞬时故障保留有限重试(封顶 4 次),并以轮次级 appPersisted 幂等守卫防止重复落库项目/版本 |
| 版本请求真取消 + 加载态(T25) | AbortController 中断挂起请求(原 Promise.race 超时无法取消底层 fetch,重试排队放大延迟),单次 4s 超时最多 3 次;面板空列表期间显示「正在读取版本历史…」,避免恢复/重试期误判「暂无版本记录」 |
| 未配置 Supabase 时演示模式(内存数据) | 仅作为本地未配置环境变量时的开发兜底,刷新即失;云端模式错误全部显式透出 |
| 单文件 HTML 应用产物 | iframe srcDoc + sandbox 直接运行,可落库、可回放、可克隆;多文件工程化生成列为后续项 |
| 主题真实生效(T12/T15) | 主题名 + 中文主题指令注入生成提示词,预览色板与 AI 产物一致;默认无选中、不注入默认主题,而非仅前端换肤 |
| 会话归属工作区(T7) | conversations 按 space 隔离,Sidebar 最近对话/新会话按当前工作区过滤;注册触发器自动创建资料与默认工作区 |
| 提交即进入详情页(T13) | 首页提交后立即创建会话并跳转 /chat/:conversationId,生成/流式/预览/队列全部收敛在详情页,首页保持轻量欢迎视图,避免双视图状态机复杂化 |
| 队列续跑(T13) | 生成中提交自动入队,当前生成结束后自动执行,输入不丢失 |

## 二、架构设计

### 1. 总体架构
- **前端 SPA**(app/frontend):React 18 + TypeScript + Vite + Tailwind + shadcn/ui + react-router v6;react-resizable-panels 实现可拖拽/可折叠侧边栏。
- **后端 Supabase**(项目 pofchtyjqwevchiiqags):Auth 复用 `auth.users`(不自建用户表或第二套登录)、Postgres + 全表 RLS、Edge Functions(AI 生成 SSE、语音转写);AI 密钥仅存 Supabase Secrets,前端不持有。
- **数据访问**:统一收敛在 `src/lib/supabase.ts`(实体 CRUD + 模板占位 + 转写);云端模式错误显式抛出,由调用侧 toast 透出。

### 2. 前端模块
| 模块 | 关键文件 | 职责 |
|------|----------|------|
| 布局 | components/layout/AppLayout.tsx、Sidebar.tsx | 分栏布局、工作区选择器、最近对话操作 |
| 首页 | pages/HomePage.tsx | 欢迎视图、提交即创建会话跳转详情页 |
| 详情页 | pages/ChatDetailPage.tsx | 生成/队列/失败卡/显式演示模式/查看器页签/回放 |
| 输入区 | components/chat/ChatComposer.tsx | + 菜单/主题/构建目标/# 引用/语音/发送停止 |
| 消息 | components/chat/ChatMessage.tsx | 用户消息/计划卡/代码进度/失败卡 |
| 预览 | components/preview/AppPreview.tsx | iframe 预览/源码/演示徽标 |
| 智能体 | lib/agent.ts | Edge Function SSE 消费、指数退避重试、显式演示智能体 |
| 演示产物 | lib/demo-apps.ts | 6 类演示应用生成器(isDemo 标记、主题色板、模板占位替换) |
| 数据层 | lib/supabase.ts | 实体 CRUD、模板占位、转写、演示兜底 |

### 3. AI 管线与 SSE 协议(Edge Function `app_atoms_agent_generate`)
四段式管线,替代「一个提示词直出 HTML」的粗放做法:
1. **意图识别 analyzeIntent**:提炼应用类型/内容分区/关键功能,模糊需求注入默认假设;
2. **布局规划 PLAN_SYSTEM**:独立小调用产出 5 步计划(布局结构/Flex-Grid/视觉规范/交互/响应式),15s 预算,失败回退启发式计划;
3. **代码硬约束 CODE_SYSTEM**:禁无约束绝对定位、统一间距圆角、强制移动端适配、纯 HTML 文档输出、行数预算;
4. **产物净化 sanitizeHtml**:剥离 Markdown 围栏与杂文,保证 iframe srcDoc 直接运行。

SSE 事件协议:`message / plan / step-* / code-delta / app / done / error`;140s 软超时,超时/流中断/HTML 不完整时主动 error + done 收尾,不静默截断。

### 4. 重试与降级链路(T21/T23)
```
用户请求
 └─ Edge Function:主模型 deepseek-v4-flash
     ├─ 瞬时错误 → 同模型重试
     ├─ 仍失败 → 备用通道 gpt-5.4 → gemini-3.1-pro-preview(各含同模型重试)
     └─ 全部失败 → HTTP 502「平台 AI 网关故障,请稍后重试」(显式文案,不静默兜底)
前端 lib/agent.ts:瞬时故障(5xx/429/网络)3 次指数退避(1.2s→2.4s→4.8s)
失败 → 失败卡(错误文案 + 指引)
     ├─ 「重新生成」原地续跑(不重复落库用户消息)
     └─ 「使用演示模式生成」显式兜底(用户主动触发,消息带【演示模式】标识)
```

### 5. 数据模型与 RLS
- 表:profiles、spaces、projects、conversations、messages、community_apps、templates、project_versions(迁移 001–006,详见 docs/data-model.md);project_versions 存版本快照(version_number/source/label/app_html),messages.metadata(JSONB)存演示标识与计划卡,projects.is_demo 标记演示产物。
- 用户数据表含 user_id 关联 `auth.users`,全表启用 RLS(本人可读写);community_apps/templates 为公共数据。
- 注册触发器(迁移 004/005):自动创建 profile + 默认工作区;hashtext 取色已修复负数下标问题。
- conversations.is_favorite + 部分索引按收藏优先排序(迁移 005);消息随会话级联删除。

## 三、当前功能完成度(T1–T29)

| 模块 | 内容 | 状态 |
|------|------|------|
| 布局与认证 | 可折叠/可拖拽侧边栏、邮箱认证、OAuth 回调、注册触发器默认工作区 | ✅ 完成 |
| 工作区与会话(T7) | 新建/切换工作区、会话隔离、新会话入口 | ✅ 完成(回归 15/15) |
| 输入区(T8/T11/T12/T15/T17/T18/T22) | + 菜单分组面板、# 引用四类、主题菜单七主题+搜索、构建/目标纯文字、语音按钮、Palette 图标、默认无选中 | ✅ 完成(回归 22/22、T22 走查 16/16) |
| AI 生成(T2/T9) | 四段管线、SSE 流式、计划卡、代码进度、iframe 预览 | ✅ 完成(T9 14/14 + 5 类质量门槛;真实 AI 全链路已于 2026-09-20 余额恢复后复测通过) |
| 主备降级与演示模式(T21/T23) | 失败卡/重新生成续跑/显式演示模式/502 显式文案 | ✅ 完成(T23 走查 14/14、故障路径实测) |
| 详情页(T13/T16/T20) | 提交即进入、队列、停止、页签、刷新回放、关联项目、失败空态 | ✅ 完成(回归 17/17) |
| 会话操作(T10) | 收藏/重命名/删除/星标排序/级联删除 | ✅ 完成(回归 20/20) |
| 增量修改与版本管理(T25) | previousHtml 增量链路、版本四时机快照、历史面板/切换/回滚/在线编辑/导出、metadata 与 is_demo 刷新一致性 | ✅ 完成(T25 走查 26/26,刷新后 v1–v4 稳定恢复) |
| 会话归档(T26) | `conversations.status` 状态字段、仅归档本人非收藏且非最近 5 条会话、列表与详情按 `active` 过滤、归档直达守卫 | ✅ 完成(T26 走查 14/14,归档仅隐藏不删除、收藏豁免生效) |
| 浅色主题体系(T27) | 详情页左侧对话栏/状态卡/失败卡/空态/队列面板、源码查看器与 HTML 编辑器、`#` 引用与 `+` 号菜单统一白色浅色语义变量 | ✅ 完成(T27 专项走查 17/17,`getComputedStyle` 亮度实测;深色用户气泡与主题预览装饰条按设计保留) |
| Agent 死循环修复(T28) | 终止守卫(app 交付后缺 done/截断补发收敛不重跑)、重试边界(未交付瞬时故障封顶 4 次)、确定性 error 不自动继续、同轮次幂等落库 | ✅ 完成(t28-loop-repro.mjs Mock SSE 11/11,修复前 4 次上游调用+4 次 plan→修复后各 1 次;真实 AI 恢复后 t9 14/14、test_agent_e2e.py 通过) |
| 真实 AI 全链路终验与产物语法自检(T29) | Edge Function 交付前对内联 `<script>` 做 `new Function` 纯语法校验,检出错误(如非法 `const` 声明)发起一次内容保全式 AI 修复重试并记录 `syntaxFixed`;刷新回放改等待式断言消除竞态 | ✅ 完成(t29-real-chain-walkthrough.mjs 真实链路 32/32,总耗时 49.5s:停止轮上游调用 1 次、两轮真实增量 T29R1MARK/T29R2MARK 特征保持、版本链 v1→v4、Preview/源码/导出一致、刷新回放消息/计划卡/真实生成徽标/4 版本/关联项目齐全、产物 iframe 无语法错误;t25 复跑 26/26) |
| 语音转写(T5) | scribe_v2 录音转写、原生识别回退 | ✅ 完成(E2E 通过) |
| 模板占位(T5) | 占位填写弹窗、内容替换、落库回放 | ✅ 完成 |
| 资源/项目页(T3) | 发现/模板过滤、体验/克隆/魔改、收藏 | ✅ 完成 |
| 详情页页签真实化 | 编辑器在线编辑(T25 已实现)、云/文件/增长空态占位 | ⏳ 部分完成(云/文件/增长仍为占位) |
| 多文件工程生成 | 单文件 HTML 之外的工程形态 | ⏳ 未实现 |
| 附件内容进上下文 | 当前仅记录文件名 | ⏳ 未实现 |
| MCP 真实协议 | 当前本地配置 + 状态徽标 | ⏳ 未实现 |

**外部阻塞复测结论(2026-09-20)**:此前 HTTP 402「AI 账户余额不足」(早期表现为 403「error code: 1010」)已解除。真实 AI SSE 全链路(`test_agent_e2e.py`,SSE 事件齐全、HTML 约 6789 字符)、T9 生成质量与浏览器全链路(`t9-agent-quality-regression.mjs` 14/14,含落库 201 与刷新回放)均复测通过;T13 应用查看器与项目落库断言、演示模式走查(t23/t25/t27)改由路由拦截模拟 402 稳定覆盖,不再依赖外部故障窗口。

## 四、计划功能优先级排期(仅计划,未开发)

| 优先级 | 事项 | 说明 |
|--------|------|------|
| ~~P0~~ ✅ | AI 账户充值/网关恢复后全链路复测 | 已完成(2026-09-20):余额恢复后 `test_agent_e2e.py` 通过(SSE 事件齐全、HTML 约 6789 字符),`t9-agent-quality-regression.mjs` 14/14(含落库 201 与刷新回放);T23/T25/T27 走查改路由拦截模拟 402 稳定复跑 |
| P1 | BYOK 用户级大模型供应商设置 | 已评估、暂缓:支持用户在设置页配置自有供应商 Key(OpenAI 兼容接口),注入 Edge Function 生成链路;含密钥加密存储与连通性测试 |
| P1 | 生成质量/速度迭代 | 更优模型 A/B、规划与代码生成并行化(「增量修改 previousHtml」已于 T25 实现) |
| P2 | 详情页页签真实化 + 项目工作区 | 编辑器(只读→可编辑)、文件树、Atoms 云资源绑定,把「生成器」升级为「可持续开发的工作区」 |
| P2 | 附件内容进入生成上下文 | 当前仅记录文件名,需解析文本/图片内容注入提示词 |
| P2 | MCP 真实协议对接 | 远程 MCP 服务真实调用与结果注入 |
| P3 | 工程化多文件生成 + 分享落地页 | 支撑真实项目形态与传播;依赖 P2 的项目工作区模型 |
| P4 | 测试 CI 化 + 部署体验 | 回归脚本容器化/定时执行,一键发布与预览环境 |

**总体判断**:产品主链路(输入 → 生成 → 预览 → 落库 → 回放 → 管理)已真实闭环,T23 后故障路径(模型降级/自动重试/显式演示兜底)已闭环,T25 补齐增量修改与版本管理(快照/切换/回滚/在线编辑/导出,走查 26/26),T28 修复生成完成后的重新规划死循环(终止守卫 + 有限重试 + 幂等落库,Mock SSE 专项 11/11),T29 完成真实 AI 全链路终验(32/32,总耗时 49.5s)并补齐产物 JavaScript 语法自检与内容保全式自动修复,消除预览 iframe 的运行时语法报错。此前唯一外部阻塞(平台 AI 账户余额不足 HTTP 402)已于 2026-09-20 解除,真实 AI 全链路(`test_agent_e2e.py`、t9 14/14、t29 32/32)复测通过,全量回归就绪。后续投入优先级:BYOK 供应商设置(P1)→ 详情页工作区化(P2)→ 工程化多文件(P3)。
